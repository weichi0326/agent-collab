# 多 Agent 协同工具 - 施工清单

> 创建日期：2026-07-02
> 基于：开发清单 v3.02
> 状态：施工进行中（M1-M7 核心已完成含 Tauri/Python/工具/Runner MVP，M5 前后台绑定与后台重启按钮已增强，`tauri:dev` 启动链路已修复，开发模式白屏已定位为 Tauri dev CSP 拦截 Vite React Refresh/热更新并叠加重复 React 插件注入，现已通过 `devCsp`、`127.0.0.1` 开发地址和去重 dev server 插件修复；运行中关闭主窗口会被拦截并提示，Tauri 主进程异常会写入 `data/crash.log`；异常退出/重启后遗留的运行中历史会在主界面渲染前自动标记为“应用已重启，任务已中断”并解除锁定，打开旧历史时也会兜底恢复，杜绝僵尸运行态复活；M6 新增 Word 读取与 Word 写入工具，`llm-calling` 瞬断/分块响应异常重试分支已修复，并新增 unittest 覆盖断流、超时、连接重置、HTTP 429 与非 JSON 响应，避免异常链路再漏测；v4 审计高危项已快修：`llm-calling` 仅允许白名单域名代理请求、`file-write` 已移除 UAC 提权修复路径、LLM 后台调用已用线程包装避免阻塞事件循环；M7 运行产物已统一固定输出到项目 `outputs/` 并按月份/日期/画布运行/节点产物分级，通用 Agent 节点已支持选择 Markdown/Word/Excel/思维导图输出格式且 `test` 画布四格式已真实跑通，每个节点会同步写出机器可读 `data.json`，内置输出 schema 校验与一次自动修复已接入，节点产物摘要已随历史快照保存并在属性面板展示，连续工作流 LLM 调用已补 429/5xx 重试和可读错误，历史回看已支持手动删除产物自动置灰删除线，运行历史列表已用左侧 2px 莫兰迪色竖线展示画布运行状态，画布页签选中态已按示意图返工为莫兰迪蓝浮起标签，运行状态可视化/节点三态耗时样式/运行副本实时快照/中止任务已落地，标题栏临时状态调试入口已移除；M8 单画布顺序/依赖并行/上游 JSON 优先流转/失败隔离/多画布批量并行/schema 匹配与 LLM fallback 已完成，M9 自然语言操作已升级为 LLM 结构化规划 + 多步确认执行，支持创建画布、加节点、连线、删节点、改输出格式与运行，M10 报告管理 MVP 已升级为报告中心）

> v2.66 施工同步：已进入架构整理与低耦合改造阶段，新增 `docs/模块化拆分规范.md`；`canvasStore.ts` 从 1026 行历史峰值拆到 645 行，新增 `stores/canvas/*` 功能节点；`masterActions.ts` 从单个 573 行左右的大文件拆成 9 行稳定门面 + `lib/masterActions/*` 细分模块。外部导入路径保持不变，现有功能不需要迁移调用。

> v2.67 施工同步：继续完成前端核心文件拆分，`agentRunner.ts` 从 1665 行拆到 501 行调度主干；`MasterAgentPanel.tsx` 从 975 行拆到 617 行会话主干；`CanvasArea.tsx` 从 578 行拆到 454 行；`PropertiesPanel.tsx` 从 638 行拆到 507 行；`ModelConfigModal.tsx` 从 530 行拆到 389 行。新增功能节点目录已同步到 `docs/模块化拆分规范.md`。

> v2.68 施工同步：按顺序完成三项继续解耦任务。`CanvasArea.tsx` 搜索与快捷键逻辑已分别抽入 `useCanvasSearch.ts`、`useCanvasHotkeys.ts`，主文件约 265 行；`PropertiesPanel.tsx` 选中节点上下文已抽入 `useSelectedNodeContext.ts`；`LeftSidebar.tsx` 从 331 行拆为 36 行页签主干 + `LeftSidebar/*` 功能节点。前端 `lint` 与 `build` 已通过。

> v2.69 施工同步：新增 `ReportCenter/` 功能节点，标题栏新增“报告中心”入口并支持工作台/报告中心主面板切换；报告模式下顶部按钮替换为返回工作台、刷新报告、打开输出目录。右侧属性面板移除节点内历史报告，历史产物统一进入报告中心统计与管理；临时状态调试工具组件已删除。前端 `lint` 与 `build` 已通过。
> v2.70 施工同步：完成一批操作体验治理。报告中心统计口径改为仅采集已结束运行，运行中画布暂不进入指标和历史产物视图；标题栏移除批量运行入口并新增当前模式提示，自然语言操作白名单同步移除批量运行；报告中心“刷新报告”改为“刷新”，输出目录入口移入历史产物区域。画布工具栏新增重命名，关闭有改动画布支持“保存并关闭”；Agent 库增加明确编辑按钮和拖拽排序；报告指标、只读快照、附件限制补充解释；模型配置把已配置项展示在新增入口之前。前端 `lint` 与 `build` 已通过。
> v2.71 施工同步：按界面复核移除右侧属性面板工具标签下方说明块；`TOOL_TAGS` 改由 `TOOL_REGISTRY` 派生，修复工具标签与工具库双维护风险。同步维护技术评审报告和交接注意事项，并完成前端 `lint/build`、Rust `cargo check --offline`、Python `py_compile/unittest` 自检。
> v2.72 施工同步：完成交互评审收尾（评审计划 Phase 7/8）共 6 项体验加固。①姬子会话首轮问答后后台生成 ≤16 字中文标题回填，失败保留截断标题；②单会话消息上限 100 条，达上限自动开延续会话继承上下文摘要，且上限校验下沉到 `masterAgentStore.addMessage`，覆盖发送/取消/确认/自定义值等所有写入路径；③姬子抽屉收起超 5 分钟卸载释放内存、展开即挂载，收起时若有会话仍在生成回复则等任务跑完再倒计时、展开立即打断，避免卸载 abort 掉在跑的回复；④会话栏正在生成回复的会话显示 loading 图标；⑤运行中画布页签隐藏关闭按钮并在状态栏提示“运行中不可关闭”，结束后恢复；⑥数据恢复期 `ensureCanvas` 不清空 `activeId` 避免僵尸空态，启动超时改为“重载 / 继续进入”二选一并说明继续进入的后果。均为已完成功能的加固与打磨，未引入新依赖。已通过 `npx tsc -b` 与 `npx oxlint`。
> v2.73 施工同步：完成总 Agent 工具管理（M6/M9 工具下载/添加/打标签），打通「姬子生成工具 → 人工审阅确认 → 动态注册 → 试运行」闭环。Python 新增 `tools/custom/`（自定义/生成工具模块 + `registry.json`）、`tools/dynamic.py`（服务启动扫描 registry 并 `importlib` 注册进 `_REGISTRY`、内置 8 工具统一元数据）、`tools/installer.py`（工具名 slug 校验+禁覆盖内置、pip 包名正则白名单防注入、`compile()`+`ast` 校验存在顶层 `async def execute`、代码 ≤20KB、`subprocess` arg 列表+超时装依赖、安装失败回滚、删除仅限自定义），`router.py` 新增 `GET /tools/meta`、`POST /tools/install`、`POST /tools/{name}/remove`，`tests/test_installer.py` 覆盖名/包校验、compile_check 拒绝分支、依赖安装与删除（22 项 unittest 全通过）。前端新增 `stores/toolStore.ts`（内置+自定义元数据合并、`useToolTags` hook 供标签选择器）、`lib/toolGenerator.ts`（造工具意图识别 + 受控 prompt 让模型输出工具 JSON），`pythonClient` 增 `listToolMeta/installTool/removeTool`，`toolRegistry` 增 `mergeToolTags`，`AgentConfigModal/PropertiesPanel` 标签选择器改用 `useToolTags`，`ToolConfigModal` 增自定义工具分组 + 「试运行」区（手填 params JSON 调 execute 看结果/错误），姬子 `create-tool` 动作在确认卡片展示完整 Python 代码块供人工审阅。安全底线：生成代码必经用户审阅完整代码后人工确认才落盘+装依赖+注册，绝不自动执行未确认代码；本轮无 Runner/姬子 function-calling，生成工具仅经「试运行」验证。已通过 `npx tsc -b`、`npx oxlint`、Python `unittest`(22)+`py_compile`。
> v2.74 施工同步：完成姬子自愈闭环（Agent 失败 → 姬子诊断 → 半自主造工具 → 重跑画布，M9 编排层扩展）。新建常驻编排层 `stores/orchestratorStore.ts`：Runner 节点失败时 fire-and-forget 上报（`agentRunner.ts` 失败分支与运行前 `ensureRequiredTools` 预检路径均上报，上报源画布 id 而非只读运行副本），姬子 `chat` 分类判断是否缺工具/库 → 已配搜索则 `searchWithFailover` 搜候选库 → `generateToolWithLLM` 生成候选工具 → `pendingActionStore.setPending` 挂起 `create-tool` 待确认并注入诊断消息；安装成功后 `onToolInstalled` 回调切回源画布并挂起 `run-active-canvas`「重跑整张画布」待确认。基础设施改造：新建 `stores/pendingActionStore.ts` 把待确认状态从 `MasterAgentPanel` 组件内 state 上提为常驻 store（AbortController 改模块级 Map，抽屉卸载不丢），新建 `lib/appNotify.ts`+`components/AppBridge.tsx` 捕获 antd `message`/`notification` hook 实例供 store 外部调用（挂在 `main.tsx` 的 `<AntApp>` 内），`uiStore` 新增 `drawerExpanded`/`view`（瞬态不持久化，`MasterAgentDrawer`/`App` 改读写 store），`masterAgentStore` 新增会话定向写入 `addMessageToSession`（异步诊断期用户可能已切会话）。抽屉可见走原确认卡片、收起走单条常驻聚合通知（带计数+「去确认」跳回并展开）。安全底线不变：编排层只诊断+生成，安装仍走确认卡片、展示完整代码、人工确认才落盘装依赖，绝无自动安装/执行。防重跑死循环：同 canvas+node+error 去重只诊断一次，重跑再失败不再触发新诊断；`MasterConfigModal` 新增「节点失败时自动诊断」开关（持久化默认开，关闭则完全静默）。已通过 `npx tsc -b`、`npx oxlint`。
> v2.75 施工同步：自检自修补丁，聚合三个 review agent 发现修复 12 项代码质量/效率问题。复用 `errorMessage` 替代手写错误提取（`orchestratorStore`/`pendingActionStore` 各 1 处）；清理任务名/阶段前缀/WHAT 步骤注释；`IncidentStatus`/`Incident`/`ReportInput` 改非导出；`onToolInstalled` 加画布存在守卫+动态 import canvasStore 改静态（验证无循环依赖）；`MasterAgentDrawer` setExpanded 改函数式+`uiStore.setDrawerExpanded` 接受 updater；`__preflight__` 抽常量 `PREFLIGHT_NODE_ID`；`setChoice`/`setCustomValue` 加 no-op guard 避免击键触发重渲染；`MasterAgentPanel` 订阅粒度改 `s.pendingActions[activeSessionId]`；`onToolInstalled`+`reportNodeFailure` 状态变更后补调 `refreshNotification`；`addMessageToSession` 去 some 守卫、set 内 find 判空 + return s no-op。已通过 `npx tsc -b`、`npx oxlint`。
> v2.76 施工同步：姬子提示词成本/长度治理 + 当前时间注入。①`llmClient.ts` chat() Anthropic 分支 `system` 改为带 `cache_control: ephemeral` 的内容块接入 prompt caching（OpenAI 兼容端点自动缓存）；②`主控agent人格.md` 20955→~11.8k 字瘦身（硬规则不减），`MasterConfigModal` `PROMPT_CHAR_CAP` 8000→14000，修复此前 13k 字被静默截断问题；③`MasterAgentPanel` 拼 prompt 首段注入 `【当前时间】<用户本地日期时间>`（放 `text` 不放 `system` 以保住缓存），修复姬子无法回答"今天/几点"。已通过 `npx tsc -b`、`npx oxlint`、`npx vite build`。
> v2.77 施工同步：联网搜索前置上下文改写。`MasterAgentPanel` 新增模块级 `condenseSearchQuery`（结合最近 4 轮对话把省略式追问补成独立检索词，回退安全），搜索改用改写后查询；人格「联网搜索」条补"资料明显不符时说明不相关别硬凑"。修复"现在呢"被搜成同名歌曲的问题。已通过 `npx tsc -b`、`npx oxlint`、`npx vite build`。
> v2.78 施工同步：Agent 导出落地（4.2）。新增 `lib/agentTransfer.ts`（`buildAgentExport` 构可移植信封 + `exportAgentToFile` save 对话框→Python file-write），`AgentLibrary` 卡片编辑/克隆/删除旁新增导出图标。信封剥离 id/时间戳、modelRef 置空留 modelId 提示。导入待后续。已通过 `npx tsc -b`、`npx oxlint`。
> v2.79 施工同步：Agent 导入落地（4.2，导出/导入闭环）。`agentTransfer.ts` 增 `parseAgentImport(text, knownTags)`：校验 `kind==='agent-export'`＋schema 不高于当前、防御性取字段、名称空则拒；工具标签按本机（内置＋已装自定义，`mergeToolTags`）过滤，缺失项剔除并回报 `droppedTags`；modelRef 恒置空、回报 `modelHint`。`AgentLibrary` 顶部新增「导入」按钮＋隐藏 `.json` file input，仿人格导入用浏览器 `fileToText` 读取（绕开 file-read 沙箱，因所选文件多在项目外）→落库→`openEdit` 打开编辑器补模型，提示剔除标签与重选模型。已通过 `npx tsc -b`、`npx oxlint`。
> v2.80 施工同步：移除 Schema 输入框 + 迁移环境修复。①按用户反馈从 `AgentConfigModal.tsx`、`PropertiesPanel.tsx` 删除「输入 Schema」「输出 Schema」两个文本框——用户期望的「上游自动产出人类可读产物 + 机器可读 `data.json`，下游自动接收」流转 Runner 早已实现，手动填 schema 匹配对个人使用无必要。**仅删 UI**：`inputSchemaText/outputSchemaText` 数据字段、`agentTransfer` 导出信封、`agentRunner/schema.ts` 校验/匹配/LLM fallback 全部保留，历史画布与已导出 `.agent.json` 不受影响，两字段现无编辑入口（保持空/沿用既有值）。②迁移遗留坑修复：`app/.cargo/config.toml` rust-lld 链接器路径 48457→Admin；`python/venv` 用本机 Python 3.12 `--clear` 重建 + 重装依赖（旧机复制的 venv `pyvenv.cfg` 指向失效 base Python，致 Python 服务起不来、前端走 Python 的操作报「请求失败」）。已通过 `npx tsc -b`、`npx oxlint`、`cargo build`。
> v2.81 施工同步：读写工具合并 + 二进制写入补全 + 旧标签迁移。①**合并读写工具对齐 Excel 多 action 范式**：`file-read`/`file-write`/`file-delete` 合并为单一 `file`(文件)，`docx-read`/`docx-write` 合并为单一 `docx`(Word)，用 `action` 参数分派。Python 新增 `tools/file_tool.py`、`tools/docx_tool.py`（委托现有模块不重写沙箱），`router.py` `_REGISTRY` 注册 `file`/`docx` 并**保留旧名作只读别名**（`listTools()` 仍含旧名→未迁移旧数据不报缺工具），`dynamic.py` `BUILTIN_META` 收敛为 5 条干净名、新增 `ALIAS_NAMES` 并入 `BUILTIN_NAMES` 做删除保护（别名不在工具库/meta 露出）。②**补全「写入二进制内容」**：`file_write.py` 新增 `_write_bytes_file` + `binary_b64` 分支（base64 解码写 bytes，走原有原子写/mkdir/权限修复路径），修复面板宣称但此前未实现的二进制写入能力。③**前端**：`toolRegistry.ts` 4 条并 2 条（`file` 12 项能力、`docx` 7 项能力，顺序 excel→file→docx→pdf-read→llm-calling）；Runner（`inputs.ts`/`outputFormats.ts`/`outputWriter.ts`/`agentRunner.ts`）工具名与 params 全部改用 `file`/`docx`+`action`。④**旧标签自动迁移**：新增 `lib/toolTagMigration.ts` `normalizeToolTags`（file-read/file-write/file-delete→file、docx-read/docx-write→docx，去重保序），接入 `agentStore`(version 1→2 migrate + preset 模板改新标签)、`canvasStore`(version 1→2 migrate 归一 canvases/savedCanvases/runHistory 节点 toolTags)、`agentTransfer` 导入过滤前归一、`CanvasArea`/`CommandPalette` 节点实例化处兜底归一。Python 别名是漏迁兜底。已通过 `npx tsc -b`、`npx oxlint`、Python `py_compile`+`unittest`(22)，并冒烟验证二进制写读往返、别名、注册表键。
> v2.82 施工同步：精简+解耦重构（无新功能、未引入新依赖）。①**`executeTool` 结果包装合并**：`pythonClient.ts` 新增 `parseHttpError`（收口 3 处 `await res.text()→JSON.parse→detail||error||body` 重复）与 `unwrapToolResult<T>(res, fallback)`（收口 5 处 `if(!res.ok) throw new Error(res.error||fallback)` 重复），`llmClient`/`agentRunner/modelCalls.ts`/`inputs.ts`/`outputWriter.ts` 5 个调用点改为 `unwrapToolResult<T>(res, fallback)`，fallback 文案逐字保留；`agentTransfer` 内联风格不动（1 处、改动比内联更噪）。②**Python 原子写合并**：新建 `tools/atomic_io.py` `atomic_write_bytes(path, data)`，把 `file_write.py` 中 `_write_text_file`/`_write_bytes_file` 两段 `mkstemp+fdopen+os.replace+异常清理` 样板合并为一处调用（text 路径先 `content.encode(encoding)` 再走 bytes helper），Append/非原子分支保持内联；`atomic_io.py` 加 5 条 unittest 覆盖基础写入/覆盖/中文往返/emoji 往返/失败清理。③**`textValue` 去重**：`masterPlanner.ts` 删本地定义，改 1 行 wrapper `sharedTextValue(v) || undefined` 保留 `undefined` 语义让 `JSON.stringify` 省 key；canonical 仍是 `agentRunner/utils.ts`。④**`nodeLabel` 重定义改 import**：扩展 `lib/agentNode.ts` `nodeLabel(node, fallback='Agent')` 接受可选 fallback；`ReportCenter/metrics.ts` 删本地 `nodeLabel`，改 `import { nodeLabel } from '../../lib/agentNode'` 并 `nodeLabel(node, '未命名节点')`；其他调用点零影响。⑤**`orchestratorStore` 解耦**：新建 `lib/orchestrator/diagnosis.ts` 收纳 `IncidentStatus`/`Incident`/`ReportInput` 类型与 `isSessionVisible`/`buildAwaitingNotification`/`buildDiagnosticPrompt` 纯函数（接受 state 作参数，结构类型化最小化接口避免导出整个 UiState/MasterState）；`orchestratorStore.ts` 顶层静态导入从 6 个 store 收敛到 2 个（仅留非循环的 `useUiStore`/`useMasterAgentStore` 供同步 handler 用），`useModelStore`/`useSearchStore`/`usePendingActionStore`/`useCanvasStore` 改动态 import；`diagnose` 函数顶部 `await Promise.all([...])` 拿 3 个 store；`onToolInstalled` 改 async + 动态 import `useCanvasStore`/`usePendingActionStore`，对齐 `pendingActionStore` 自己的 fire-and-forget precedent；`visibleForSession`/`refreshNotification` 主体改调 `diagnosis.ts` 纯函数。全项目唯一静态循环（orchestratorStore ↔ pendingActionStore）改为双向动态 import。`enabled` flag、`reportNodeFailure` 契约、`onToolInstalled` 回调契约、`diagnosedKeys` 去重、通知 key `orchestrator-awaiting` 全部不变。已通过 `npx tsc -b`、`npx oxlint`、Python `py_compile`+`unittest`(5)。
> v2.83 施工同步：节点属性面板 5 项体验修复（无新依赖）。①**数据来源 chips 横排**：`App.css` `.node-source__filelist` 从 `flex-direction:column` 改为 `row+wrap`，gap 6px 8px，多个文件 chips 横排、溢出才换行。②**修复「打开节点产物目录」失效**：`src-tauri/src/storage.rs` `open_path` 命令加 `target.is_dir()` 分支，目录用 `explorer.exe <path>` 直开（对齐 `open_output_dir`），避开 PowerShell `Start-Process -LiteralPath` 对目录的不稳定行为；文件分支保持现状（PowerShell `Start-Process` 走默认程序）。③**产物目录路径预览改卡片样式**：`PropertiesPanel.tsx` 把行内接在句末的 monospace 路径拆出 `.node-folder-preview` 独立卡片，文案精简；`App.css` `.node-folder-preview` 加 `margin-top`/`padding`/`background`/`border`/`border-radius`/`font-family` 卡片样式，`word-break:break-all` 防溢出。④**文件命名改用内容概括**：`agentRunner/outputWriter.ts` 新增 `deriveFileSubject(content, label, format, structured?)` helper——docx/mindmap 用 `structured.title`、xlsx 用 `structured.sheet`、markdown 用首个 H1（`/^#\s+(.+?)\s*$/m`）、全部回退到 label；`fileName`/`path` 计算从顶部挪到各 format 分支内 `structuredOutput` 赋值之后（避免引用 undefined）；folder 名仍用 label（按节点聚合合理）；`safeFileName` 已有 48 字符截断与非法字符替换，直接复用。⑤**data.json 不入产物列表**：`lastOutput.items` 只放主产物 `[{name, path, summary}]`，data.json 仍写入磁盘供下游节点/历史产物功能读取，但不再展示在「本次输出内容」列表；`outputFolderPath` 计算仍能从 items 命中主产物 path，`outputCheckKey` 少一个路径要检查（更轻），`listOutputReports` 走磁盘扫描不依赖 items，下游 `collectInput` 从内存 `outputs: Map` 读 `structuredData` 不依赖 items。已通过 `npx tsc -b`、`npx oxlint`、`cargo check`。
> v2.84 施工同步：节点属性面板 3 项续修（接 v2.83 用户反馈）。①**chips 横排宽度修复**：v2.83 把 `.node-source__filelist` 改 row+wrap 后，chip 内 `.node-source__file-name` 的 `flex:1` 让 chip 被推大、一行只放 1-2 个看起来仍像双排。改 `.node-source__file-chip` 加 `max-width:100%`+`min-width:0`，`.node-source__file-name` 的 `flex:1` 改 `flex:0 1 auto`+`max-width:240px`，chip 按内容宽度、长文件名 240px 内省略，多个 chips 真正横排充满。②**文件 hint 条件显示**：`PropertiesPanel.tsx` 数据来源 file 分支的 `node-hint` 原无条件显示「已选真实文件路径」，没选文件时也提示。改为 `files.length>0` 时显示「已选 N 个文件，运行时按文件路径读取内容」，没选时显示「未选择任何文件，该节点缺少数据来源，无法运行」与运行预检一致。③**没数据来源节点禁止运行**：`agentRunner/inputs.ts` 新增 `ensureDataSources(canvas)` 同步预检——遍历节点，有上游（edges 里 target===nodeId）跳过，否则按 `dataSourceMode` 检查 file/url/history 三种手动数据源是否非空，缺数据来源的节点名收集后 throw「以下节点没有数据来源，无法运行：X、Y。请为这些节点选择文件/网页 URL/历史产物，或连接上游节点后再运行。」；`agentRunner.ts:108` topoSort 后、`ensureRequiredTools` 之前调 `ensureDataSources(sourceCanvas)`，throw 直接冒泡到 `TitleBar.tsx` 的 `message.error` 提示用户，不走 orchestratorStore（那是工具缺失专用通道）。已通过 `npx tsc -b`、`npx oxlint`。
> v2.85 施工同步：严格工具门控 + 姬子自动补工具标签。修复用户发现的「取消节点文件读写标签仍能成功产出」——排查确认 toolTags 此前**不参与运行时工具调用**（Runner 三处调工具全写死，只有 `ensureRequiredTools` 读标签且仅做「工具是否已注册」的可用性加法校验，不做限制），故取消标签无约束。做成**真正的严格门控**并验证姬子自动补权限重跑。①**门控核心**（`agentRunner/inputs.ts`）：导出常量 `MISSING_TOOL_TAG_MARKER='缺少工具标签'`（门控消息与姬子识别共用）；`requiredToolTagsForNode(canvas,node)` 算必需标签集 = 产物写工具（`outputSpecForFormat(outputFormatForNode(node)).tool`）+ 输入读工具（仅无上游时对 `dataSourceFiles`+`dataSourceHistoryPaths` 走 `toolForInputPath`）；`ensureNodeCapabilities(canvas,node)` 用 `normalizeToolTags` 归一节点声明标签后与 required 集合比对，缺失即 throw 以 MARKER 开头、列中文 label 的错误。**刻意豁免** `llm-calling`（Agent 本质能力）与 `data.json` 内部 `file` 写入（系统信封），docx/xlsx 节点只需勾对应格式工具。②**门控位置**（`agentRunner.ts`）：`executeNode` try 内、重试循环前调 `ensureNodeCapabilities`（配置错误不进重试），失败走既有 catch→`reportNodeFailure`（携真实 nodeId+源画布 id）；门控错误不匹配 `RETRYABLE_NODE_ERROR_PATTERNS`。③**PREFLIGHT_NODE_ID 上提**（`agentRunner/constants.ts`）：从 `agentRunner.ts` 局部常量移到 constants 纯模块，供编排层静态导入区分「预检失败」与「具体节点失败」而不成环。④**姬子自动补标签**（`orchestratorStore.ts`）：`reportNodeFailure` 创建 incident 后改调 `void tryAutoGrantThenDiagnose(incident)`——`tryAutoGrantToolTags` 用 MARKER 识别门控失败（且 `nodeId!==PREFLIGHT_NODE_ID`），动态 import canvasStore/pendingActionStore/inputs/pythonClient/toolRegistry，定位源节点并**重算** required（不靠字符串解析）得缺失集，`await listTools()` 校验缺失标签**全是已注册现成工具**才**自动补标签**（`updateNodeData` 写回源画布去重合并）+写姬子消息+复用 `onToolInstalled` 同款重跑流程（setActive+`run-active-canvas`，choice:confirm）+`patchIncident(awaiting-confirm)`；否则回落 `diagnose()` 造新工具。决策：补已存在良性标签自动应用、只对重跑保留人工确认，RCE 硬底线（代码生成/安装/执行）不受影响；姬子关闭则早返回、节点带清晰提示失败供手动勾选。已通过 `npx tsc -b`、`npx oxlint`。剩余 6 项运行时 UI 冒烟测试待用户手动验证（门控拦截缺 file 标签的 markdown 节点 / 姬子自动补并确认重跑 / 姬子关闭时干净失败+手动修 / docx 节点只需 docx 标签 / .pdf 输入读工具门控 / 真缺能力回落造工具）。
> v2.86 施工同步：姬子自愈两档区分——补已注册工具「零确认 + 自动重跑 + 画布内红框提示」，安装新工具仍需人工确认；自愈行为计数入报告中心。接 v2.85 用户反馈，把「补已注册工具标签」（无代码风险）与「安装新工具」（RCE 硬底线）拆成两档处理。①**数据模型**：`stores/canvas/types.ts` `Canvas` 加 `autoToolNotice?:{text;dismissed}`，随 `canvases` 持久化。②**提示源画布→只读快照转移**：`canvas/runRecords.ts` `createRunningArtifacts` 造运行副本 tab 时带 `autoToolNotice:canvas.autoToolNotice`；`canvasStore.ts` `createRun` 追加 tab 同时清源画布 notice，使提示落到用户实际看到的只读运行快照。③**canvasStore action**：新增 `setAutoToolNotice(canvasId,text)`/`dismissAutoToolNotice(canvasId)`（「收起」置 dismissed，只读快照 tab 在 canvases 里可改）。④**浮层**：新建 `components/CanvasArea/AutoToolNotice.tsx`（`<Panel top-right>` 红框卡片 + 「收起」），`CanvasArea.tsx` Flow 在 `autoToolNotice && !dismissed` 时渲染，`App.css` 加 `.canvas-auto-notice` 样式（浅红底 + `#ff4d4f` 边框）。⑤**orchestratorStore 零确认 + 计数**：state 加持久化 `autoGrantedToolCount`/`installedToolCount`（初值 0，`merge` 兜底 `?? 0`）+ `recordToolInstalled()`；`tryAutoGrantToolTags` 补标签成功分支改为累计计数 + `setAutoToolNotice` + 陈述句消息 + `patchIncident(resolved)` + `setPending(run-active-canvas,confirm)` 后**立即** `void runPending(sessionId,'confirm')` 零确认自动重跑，删补标签分支的 `refreshNotification`。⑥**install 计数**：`pendingActionStore.ts` `runPending` 中 `create-tool` 成功后无条件 `recordToolInstalled()`（失败走 catch 不计数），再按 incidentId 触发 `onToolInstalled`。⑦**报告中心**：`ReportCenter.tsx` select 两个原始计数，`.report-stats` 加「姬子自动补工具」「姬子安装工具」两卡 + Tooltip。RCE 硬底线不变（只对补已注册标签+重跑零确认，无代码生成/安装/执行）；死循环防护沿用 `diagnosedKeys`；真缺能力仍回落 `diagnose()` 确认链路。已通过 `npx tsc -b`、`npx oxlint`。剩余端到端冒烟待用户手动验证（补标签零确认+红框提示 / 提示常驻+收起持久化 / install 仍需确认 / 计数入报告中心 / 姬子关闭不自动补）。
> v2.87 施工同步：姬子补已注册标签后「就地重跑失败节点及其下游」（不新开画布）。接 v2.86 用户反馈「能不能不新开画布，直接重试失败子节点，重开画布感觉不太好」；AskUserQuestion 确认目标为**只重跑失败节点 + 其下游**（上游已成功节点保持不变、不重算），**就地在既有只读运行快照 tab 上重跑**，无法就地时回落 v2.86 整图重跑保证不倒退。①**Incident/ReportInput 加运行副本定位**（`lib/orchestrator/diagnosis.ts`）：两接口各加可选 `runTabId?/runId?`（只读运行副本 tab id 与 runId；预检失败为空→触发回落）。②**失败上报带定位**（`agentRunner.ts`）：节点失败 catch 的 `reportNodeFailure` 追加 `runTabId:canvas.id, runId`（作用域内本就同时持有运行副本 tab id 与 runId，此前只传源 canvasId）；预检失败分支不加（无 run）。③**抽取共享执行核心 `runGraph`**（单一事实源供整图+子图复用）：把 `runCanvas` 内嵌 Promise 调度器 + 状态初始化抽成内部 `runGraph({canvas,runId,sourceCanvasId,order,incoming,outgoing,byId,outputs,presetSuccess,runStartedAt,signal})`；`presetSuccess` 预置 `status='success'`（视为已成功不调度），ready 初值判定泛化为 `parents.every(p=>status==='success')`（整图根节点无入边天然成立、行为零变化），`settled/total` 以 `order.length` 计；`runCanvas` 改薄封装（预检/createRun/topo/边索引后调 `runGraph`，order=全图、outputs/presetSuccess 空，对外行为完全不变）。④**`rerunCanvasNode`+`restoreNodeOutput`**（`agentRunner.ts` 新增导出 / `agentRunner/types.ts` 新增 `RerunUnavailableError`）：校验运行副本 tab（`readOnly&&runId`、目标 node 存在）否则抛 `RerunUnavailableError`；`order`={nodeId}∪全部下游后代（outgoing BFS 再 topoSort）；子图上游（入边来源不在 order 内）经 `restoreNodeOutput` 从磁盘 `data.json`（`joinPath(getAppOutputDir(),lastOutput.folderName,'data.json')`，取 envelope `node.label/rawReply/data/summary` 四字段=collectInput 所需）恢复进 `outputs`，任一失败抛 `RerunUnavailableError`，`presetSuccess`=全部非重跑节点；order 内节点状态回 `queued`+挂运行态+syncRunSnapshot 后调 `runGraph`。⑤**新 MasterAction `rerun-canvas-node`**：`masterActions/types.ts` union 加 `{type:'rerun-canvas-node';runTabId;nodeId;sourceCanvasId}`；`executor.ts` 加分支调 `rerunCanvasNode`；`descriptions.ts` 补 case（无 default 穷举 switch）。⑥**orchestratorStore 优先就地重跑**：`tryAutoGrantToolTags` 成功分支双写标签（源画布修复未来整图 + 运行副本让本次门控过）后判 `canPartial`（`runTabId&&runId` 有值、运行副本 tab id+runId 都对得上、tab 上有目标 node）——命中则 `setAutoToolNotice(runTab.id)` 直接挂运行副本、陈述句改「就地重跑失败节点及其下游（不新开画布）」、setPending `rerun-canvas-node`+`setActive(runTab.id)`+`runPending(confirm)` 零确认；不命中回落 v2.86 整图重跑（`setAutoToolNotice(源)` 靠 createRun 转移 + `run-active-canvas`）。`create-tool` 安装路径完全不动（RCE 硬底线：仍人工确认完整代码）；死循环防护沿用 `diagnosedKeys`。已通过 `npx tsc -b`、`npx oxlint`。剩余端到端冒烟待用户手动验证（就地重跑主路径：失败节点+下游重跑、上游保持不重算、不新开画布 / 提示常驻+收起 / 关闭运行副本 tab 后回落整图 / install 仍需确认 / 整图运行回归含并发失败隔离中止 / 计数入报告中心 / 姬子关闭不自动补）。
> v2.88 施工同步：Python 子进程日志落盘（可诊断慢跑/重试）。此前 Rust 拉起 uvicorn 子进程时未重定向 stdout/stderr，日志直接丢弃，且 `llm_calling` 重试是「静默」`time.sleep+continue`，导致长任务慢跑/瞬断无从复盘。①**Rust 落盘**（`src-tauri/src/python_manager.rs`）：新增 `open_python_log()`——在项目根 `logs/` 下以 append 模式打开 `python.log`（保留跨重启历史），超 5MB（`PYTHON_LOG_MAX_BYTES`）先滚动到 `python.log.1` 再重开；`start()` spawn 前设 `PYTHONUNBUFFERED=1`（否则重定向到文件是块缓冲、日志要等崩溃才刷出），并把子进程 stdout/stderr 用同一文件句柄克隆重定向到该文件，打不开日志时回退 `Stdio::null` 不阻断服务。②**应用日志配置**（`python/app.py`）：新增 `logging.basicConfig(level=INFO, format="%(asctime)s %(levelname)s [%(name)s] %(message)s")`，让 router/llm_calling 等应用模块日志统一走 root logger 输出到 stderr（→ 落盘）。③**重试可观测**（`python/tools/llm_calling.py`）：HTTP 429/5xx 重试分支与连接瞬断重试分支均补 `logger.warning`（含状态码/异常类型、第几次重试、延迟秒数、请求体约 KB），最终放弃前补 `logger.error`，把「静默重试」变为可追踪。④**工具耗时**（`python/tools/router.py`）：`execute_tool` 记录 `time.monotonic()` 起止，成功/业务失败/系统异常三分支分别 `logger.info`/`logger.info`/`logger.exception` 输出工具名+耗时 ms。已通过 `cargo check --offline`、Python `py_compile`。剩余验证：跑一次长任务后查看 `logs/python.log` 确认重试/耗时日志成形（用户手动）。

> v2.89 施工同步：应用生命周期完善——稳健三件套 + 单实例锁 + 窗口状态持久化 + 常驻服务状态指示灯。审计发现启动主链路多处缺口：`tauri-plugin-log` 依赖早已引入却从未 `.plugin()` 注册，导致 v2.88 起 `python_manager.rs` 里大量 `log::*` 宏无处落盘（Rust 侧日志与 `python.log` 配套缺失）；退出只靠 `WindowEvent::Destroyed` 无 `RunEvent::ExitRequested` 兜底；冷启动不清端口孤儿，上次残留旧版本 Python 会被 `is_service_port_open` 直接复用而跑旧代码；无单实例锁，双开两进程抢写同一份项目明文 JSON；窗口尺寸/位置不持久化；`getServiceStatus()` 已导出无组件消费。①**Cargo**（`src-tauri/Cargo.toml`）：加 `tauri-plugin-window-state = "2"`（主 deps）、`tauri-plugin-single-instance = "2"`（`cfg(not(android/ios))` target，桌面专用）；本地缓存缺新 crate，`cargo fetch` 拉取后再 `cargo check --offline`。②**日志落盘**（`src-tauri/src/lib.rs`）：新增 `build_log_plugin<R>()`——`tauri_plugin_log::Builder` INFO 级、`max_file_size(5MB)`、`RotationStrategy::KeepOne`、`Target::new(TargetKind::Stdout)`，并把 `app_base_dir()?.join("logs")`（与 `python.log` 同目录）作为 `TargetKind::Folder{file_name:Some("tauri")}`，拿不到路径时只留 Stdout 不阻断。③**退出守卫**（`lib.rs`）：`use tauri::{...,RunEvent}`，构建器由 `.run(generate_context!())` 改为 `.build(generate_context!()).expect(...).run(|app_handle, event| if let RunEvent::ExitRequested{..}=event { python_manager::stop(state) })`，正常/异常退出都杀 Python 子进程防孤儿占用 18081，保留 `WindowEvent::Destroyed` stop 做双保险。④**冷启动清孤儿**（`lib.rs` setup + `python_manager.rs`）：`stop_orphan_python_listeners`（Windows 实现 + 非 Windows 空 stub）改 `pub`，setup 内 `start()` 之前调用（仅杀 cmdline 匹配 `uvicorn`+`app:app`+`--port 18081` 的进程，不误杀他者）。⑤**单实例锁**（`lib.rs`）：`#[cfg(desktop)]` 块内 `let mut builder = tauri::Builder::default(); builder = builder.plugin(single_instance::init(|app,_,_| { get_webview_window("main").unminimize()+set_focus() }))` 最先注册，第二进程走回调聚焦后退出、不进 setup 拉第二个 Python。⑥**窗口状态**（`lib.rs` + `capabilities/default.json`）：`.plugin(window_state::Builder::default().build())`，permissions 加 `"window-state:default"`；首次运行用 `tauri.conf.json` 默认尺寸，之后记住尺寸/位置/最大化。⑦**指示灯**（`components/ServiceStatusDot.tsx` 新增 + `TitleBar.tsx` + `App.css`）：组件本地 `useState<ServiceStatus>('starting')`+`useEffect`（挂载即查 + 每 8s 轮询 `getServiceStatus()` + 卸载 `clearInterval` + `alive` 标志防卸载后 setState），纯本地 state 不接 zustand（不违反 selector 稳定性硬底线）；`Tooltip`（运行中/启动中/未启动）+ `<span class="service-dot service-dot--{status}">`，`TitleBar` 工作台分支在「重启后台」按钮左侧渲染；`App.css` 加 `.service-dot` + `--running/starting/stopped` 三色（绿/黄/红 + box-shadow）。风险控制：single-instance 必须最先注册；冷启动清孤儿即使上次是健康同版本后台也会杀+重起一次（可接受，直接消除复用旧代码隐患）。已通过 `cargo check --offline`、`npx tsc -b`、`npx oxlint`。剩余验证（用户手动端到端）：冷启动窗口正常 + Python 拉起 + 指示灯转绿 + `logs/tauri.log` 生成；留旧 Python 监听 18081 后启动应用观察旧进程被清；应用开着再双击图标只聚焦不新开；正常关窗后 `netstat` 确认 18081 无残留；改窗口大小/位置/最大化后关闭重开恢复。

> v2.90 施工同步：Windows 安装包打包（开发清单最后一项）。目标做标准 NSIS 安装包，安装范围 **currentUser**。两大核心难题：①**绿色便携架构与只读安装目录冲突**——release 下 `data/`(明文 API Key)、`outputs/`、`logs/`、`python/` 全在 exe 同级且运行时要写（姬子往 `python/tools/custom/` 写生成工具、`installer.py` 用 pip 装依赖）。解法选 currentUser 安装（`installMode:"currentUser"`，装到 `%LOCALAPPDATA%\Programs\multi-agent-tool`、无 UAC、目录用户可写），冲突消解、架构原样成立，`storage.rs`/`lib.rs` 数据日志根与 `dynamic.py`/`installer.py` 全不用改。②**Python 不可分发**——开发 `python/venv/` 的 `pyvenv.cfg` 硬编码开发机 base Python 绝对路径，拷别机即失效；姬子动态造工具依赖运行时 pip，PyInstaller 冻结会破坏该功能。解法用 python-build-standalone（可重定位、自带 pip 的独立 CPython 3.12）。**施工步骤**：①**新增 `python/build-dist.mjs`**（Node 22 脚本，发版跑一次、需联网）：`fetch` GitHub `astral-sh/python-build-standalone` latest release → 正则选 `cpython-3.12.x+日期-x86_64-pc-windows-msvc-install_only.tar.gz` 取补丁号最高 → 流式下载到 `python-dist/` → `tar --force-local -xzf`（`--force-local` 必需，否则 `C:\` 盘符冒号被 tar 误当远程 host）解压顶层 `python/` 搬到 `python-dist/runtime/` → `runtime/python.exe -m pip install -r requirements.txt` → 拷 `app.py`+`requirements.txt`+`tools/`（递归 filter 排除 `__pycache__`/`tests/`，`custom/` 只留 `__init__.py`，`registry.json` 重置 `[]`）→ 递归算体积打印；开头先 `rm -rf python-dist` 保证可重复运行。②**`tauri.conf.json`**：`bundle.resources:{"python-dist":"python"}`（Tauri 把 `python-dist/**` 复制到安装目录 `python/`，与 `find_project_root()` 的 `current_exe().parent()` 一致）、`targets` `"all"`→`["nsis"]`（msi/wix 对 currentUser+大资源支持差不出）、新增 `bundle.windows.nsis:{installMode:"currentUser",languages:["SimpChinese","English"]}`+`publisher`/`copyright`。③**`python_manager.rs`**：`find_python_exe()` release 分支 `python/venv/Scripts/python.exe`→`python/runtime/python.exe`（dev 分支保持 venv 不动，唯一运行时代码改动）；`find_app_dir()` 不变（仍 `python/`，`app.py` 在其下 uvicorn `--app-dir` 能导入）。**验证结果**：`node python/build-dist.mjs` 实测选中 `cpython-3.12.13+20260623`，下载 43.9MB，pip 装 33 包，python-dist 总 **261.2MB**；结构校验 `runtime/python.exe`、`Lib/site-packages/{fastapi,pymupdf,fitz}`、`tools/custom/{__init__.py,registry.json=[]}` 全就位；`cargo check --offline` 通过（tauri build-script 的资源路径存在性校验过关，`python_manager` 改动编译无误）。**发版执行步骤（用户，需联网+耗时）**：①`node python/build-dist.mjs` 生成 python-dist；②`cd app && npm run tauri:build` 出 `target/release/bundle/nsis/multi-agent-tool_<ver>_x64-setup.exe`；③安装后校验安装目录内 `python/runtime/python.exe`+`python/app.py` 就位。**注意**：未签名安装包首次运行可能触发 SmartScreen「未知发布者」需点「仍要运行」；NSIS 包预计 ~120-180MB（离线开箱即用 + 保留运行时 pip 造工具的必然代价，日后瘦身可改首次运行联网下载 Python）；`python-dist/` 是构建产物不纳入源码维护；`build-dist` 要联网下载，下载后离线可重复 `tauri:build`。剩余端到端手测待用户在干净目录/无 Python 机器执行（双击安装无需管理员 / 启动指示灯转绿 + 日志生成 / 跑画布产物写安装目录 outputs / 姬子造工具验证 custom 可写+pip 可装 / 关窗后 18081 无残留 + 单实例 / 卸载）。

> v2.91 施工同步：姬子意图误判修复。现象——用户输入疑问句"你是通过什么方式创建画布并放置节点并连接的"被误判为"创建画布"操作，还抽出垃圾画布名"并放置节点并连接的"；用户明确"不要提到画布就创建，要判断真实意图"。根因双重：①`planMasterActionWithLLM` 对"LLM 判定为聊天"与"调用失败"都返回 `null`，调用方 `plannedAction ?? parseMasterAction(text)` 把 LLM 正确的聊天判断也覆盖成贪婪规则兜底；②规则 `create-canvas` 正则对疑问句照样匹配、尾串 `(.+)` 原样当名字。三点改动：①`lib/masterPlanner.ts` 新增判别联合 `PlanOutcome`（`{kind:'action';action}|{kind:'chat'}`），`planMasterActionWithLLM` 返回 `Promise<PlanOutcome>`，两处 `return null`→`{kind:'chat'}`、末尾→`{kind:'action',action}`，调用失败仍 throw（调用方 catch 回落）；②`buildPlannerPrompt` 加规则：疑问/解释语气即使含"画布/节点"也一律 `intent:chat`；③`components/MasterAgentPanel.tsx` 引 `plannerSaysChat` 标志三态分流，`plannedAction ?? (plannerSaysChat ? null : parseMasterAction(text))`——LLM 判聊天跳过兜底；④`lib/masterActions/parser.ts` 加 `looksLikeQuestion` 顶部短路 + `isValidCanvasName` 清洗画布名（含操作词或 >20 字即判无效、创建无名画布不塞垃圾名）。取舍：个人工具宁可漏判（当聊天）不误判（擅自改画布）。已通过 `npx tsc -b`、`npx oxlint`。纯前端改动，未动 Rust/Python/依赖，不影响 v2.90 打包链路。

> v2.91 待办收敛（同日决策，纯清单同步，无代码改动）：与用户逐项确认后收敛剩余待开发功能。①**取消报告内置 Markdown/HTML 预览与下载**——已有「系统默认程序打开主产物/data.json + 定位文件夹」足够，不自建预览器（改 10.2/10.3/M10）。②**取消 Markdown→JSON 反向同步**——Runner 已自动写出「人类可读主产物 + 机器可读 data.json」双产物，人工回改 Markdown 反向同步收益低、易与自动流转冲突（改 10.3/M10）。③**文件上传判定为等价完成**——节点「数据来源」file 模式经 `plugin-dialog` 选真实路径、运行时按扩展名读 .txt/.md/.docx/.pdf 作输入，即 Phase 2「文件上传」的等价实现（改 7.3）。④**Agent 自定义输出 schema 保留代码接口**——`inputSchemaText/outputSchemaText` 字段与 `agentRunner/schema.ts` 校验/匹配/LLM fallback 逻辑全部保留，但不再补编辑 UI、不作待办功能追踪；自动流转 + 内置输出 schema 校验已覆盖绝大多数场景（改 7.2）。⑤**画布级导出/导入进入方案设计**——需连带 Agent 快照数据导出（否则导入后是空能力节点），复用 `agentTransfer.ts` 信封范式，方案待批准后实施。

> v2.92 施工同步：画布级导出/导入落地（连带 Agent 快照，接 v2.91 ⑤ 的方案设计）。设计决策（已确认）：导入**仅落节点实例**不入 Agent 库、不生成 savedCanvas（符合两层模型/复制粘贴一致）；导出入口在「已保存画布」列表每条一个图标（对齐 `AgentLibrary` 卡片导出位置）；`modelRef` 恒清空留 `modelHint` 供重选；本机路径与运行态字段全部剥离。①**新增 `app/src/lib/canvasTransfer.ts`**（照搬 `agentTransfer.ts` 范式）：常量 `CANVAS_EXPORT_KIND='canvas-export'`/`CANVAS_EXPORT_SCHEMA=1`；类型 `CanvasExportEnvelope{kind;schema;exportedAt;canvas{name;nodes:ExportNode[];edges:ExportEdge[]}}`（`ExportNode` 保留原始 id 仅用于连边映射；`ExportEdge` 用原始 source/target、不带 edge.id）；`exportNodeData(data)` 白名单挑 label/description/systemPrompt/toolTags/inputSchemaText/outputSchemaText/outputFormat + 仅 `dataSourceMode==='url'` 时保留 url 数据来源 + `modelHint=data.modelRef?.modelId ?? null`，剥离 dataSourceFiles/historyPaths/outputPath/lastOutput/runState/派生 UI 态；`buildCanvasExport(input)` 逐节点映射 id/type/position/data + 边；`exportCanvasToFile(input)` 逐字照搬 `exportAgentToFile` 写盘通道——`isTauri()` 守卫 + `plugin-dialog save({defaultPath:'<safeName>.canvas.json'})` + `executeTool('file-write',{mode:overwrite,mkdir,atomic,allow_outside_roots:true})`；`parseCanvasImport(text, knownTags)` 校验 `kind===CANVAS_EXPORT_KIND`、`schema>本机即拒`、`canvas.nodes` 为数组、`name` 非空，用 `idMap`（照搬 `useCanvasHotkeys` 粘贴逻辑）重造 `uid('node')`/`uid('edge')`、`normalizeToolTags`+本机 `knownTags` 过滤（未知进 `droppedTags`）、`modelRef:null`（有 `modelHint` 则 `clearedModelCount++`）、丢弃两端映射不到的悬空边，返回 `{name,nodes,edges,droppedTags,clearedModelCount}`。②**`canvasStore.ts` 新增 `importCanvas(name,nodes,edges):boolean`**：工厂签名 `(set)`→`(set,get)`；仿 `openSaved` 但**不带 savedId**——`get().canvases.length>=MAX_CANVASES` 返回 false，否则 `recomputeDerived` 后造 `Canvas{id:uid('c'),name,nodes,edges}` push + set active（导入即未保存新 tab，用户可另存）；接口补方法签名。③**`SavedCanvases.tsx`**：每条 `history-item` 在 Edit/Delete 旁加 `ExportOutlined`（`stopPropagation`→`onExport` 调 `exportCanvasToFile`，loading/成功/失败走 antd message）；`panel-body` 顶部加「导入画布」`Button`+隐藏 `<input type=file accept=.json>`（`fileToText` 读盘绕沙箱→`knownTags=mergeToolTags(useToolStore.getState().customTools).map(t=>t.value)`→`parseCanvasImport`→`importCanvas`，达上限/剔除标签/N 个节点待重选模型均有提示）。`knownTags` 在事件回调内即时取 `getState()` 不放 selector（遵守 zustand 稳定性硬底线）。纯前端、无新依赖、无 Rust/Python 改动，不触碰 v2.90 打包链路。已通过 `npx tsc -b`、`npx oxlint`。剩余端到端手测待用户（导出含 systemPrompt/toolTags/modelHint 且不含本机路径 / 导入新开 tab 节点带能力+连线正确+模型待选 / 补模型后运行产物正常 / 非本工具 JSON 友好报错 / 画布达上限提示无法新建）。

> v2.93 施工同步：画布导入自检自修（接 v2.92，独立 review agent 审计后加固）。审查结论：核心正确性（id 重映射、悬空边丢弃、字段剥离白名单、modelRef 清空、zustand selector 稳定性、message.loading hide 必调）扎实且与 `agentTransfer.ts` 对齐；发现并修复 4 项对**不可信导入文件**（系统边界）的健壮性缺口，全部落在 `canvasTransfer.ts` `parseCanvasImport`：①**outputFormat 合法值归一**——新增 `OUTPUT_FORMATS` + `normalizeOutputFormat(v)`，导入 `outputFormat` 只接受 markdown/docx/xlsx/mindmap，非法值归 `undefined`（此前 `d.outputFormat` 是 JSON `as` 断言的任意值直塞节点 data，运行期虽被 `outputFormatForNode` 兜底成 markdown，但违反 `AgentOutputFormat` 类型契约且属性面板 Select 显示空）；②**position 有限性过滤**——新增 `finiteOr(v,fallback)`，`Number.isFinite` 过滤 NaN/Infinity（此前仅 `typeof==='number'` 会放行 NaN，致 React Flow 节点定位到无穷远）；③**节点数上限**——`IMPORT_MAX_NODES=500`，超限抛「节点数超过上限,文件可能已损坏」（防百万级 nodes 卡死渲染）；④**画布名截断**——`asString(name).trim().slice(0,40)` 对齐重命名弹窗 `maxLength={40}`（防超长/异常名撑破页签 UI）。刻意不改的可辩护项：`clearedModelCount` 只计导出时带过模型的节点（语义=「这些节点丢了模型配置」，合理）、导入达上限在解析后判（parse 成本可忽略、避免重复 `MAX_CANVASES` 逻辑）、`asString` 与 agentTransfer 各自私有（抽取属 churn）。纯前端、无新依赖、无 Rust/Python 改动。已通过 `npx tsc -b`、`npx oxlint`。

> v2.93 发版 build 已实际执行（接 v2.90 打包链路，此前标注「待用户执行」）：python-dist 经核验已完整（274M，`runtime/python.exe` + `Lib/site-packages/{fastapi,fitz,uvicorn}` + `tools/custom/registry.json=[]` 齐全，且 v2.92/v2.93 全是前端改动、无 Python 变更，故跳过重新下载 CPython+pip 一步）；直接 `npm run tauri:build`——前端 `tsc -b && vite build` 通过、Rust release 编译 1m18s、NSIS 自动下载 nsis-3.11 + nsis_tauri_utils.dll 并打包成功，产出 `app/src-tauri/target/release/bundle/nsis/multi-agent-tool_0.1.0_x64-setup.exe` **63M**（NSIS 压缩后远小于 python-dist 原始 274M，也低于 v2.90 预估的 120-180MB）。构建日志无 error。剩余待用户手动端到端安装测试（干净目录/无 Python 机器双击安装无需管理员 / 启动指示灯转绿 + 日志生成 / 跑画布产物写安装目录 outputs / 姬子造工具验证 custom 可写+pip 可装 / 关窗后 18081 无残留 + 单实例 / 卸载）。

> v2.94 施工同步：多模态图像输入链路（用户问「复杂 Excel 策划案又有美术示意图、又有文字描述、甚至有各样批注，当前 Agent 节点是否无法处理」；确认走多模态、后期换多模态大模型；AskUserQuestion 拍板：范围=完整链路一次做完[通用图片地基＋Excel 自动抽图/抽批注]，非视觉模型=运行前硬拦截报错[正面回应「图被安静跳过却以为全看了」]）。改动前现状：`excel_tool.read_sheet` 只 `iter_rows(values_only=True)` 取文本、内嵌图片＋批注被静默丢；整条 LLM 链纯文本（`collectInput` 返 `string`、`modelCalls` 硬编码 `content:prompt`），抽出图也喂不进模型。**施工步骤**：①**`python/tools/excel_tool.py`**——顶部加 `import base64/zipfile`＋护栏常量 `MAX_XLSX_IMAGES=50`/`MAX_XLSX_IMAGE_SIZE=8MB`/位图后缀白名单 `_IMAGE_MEDIA_TYPES`（png/jpg/jpeg/gif/bmp/webp→media_type）；抽出辅助 `_read_rows`（供 read_sheet/read_rich 共用）、`_extract_comments`（openpyxl `cell.comment.text/.author` 遍历目标或全部 worksheet）、`_extract_images`（`zipfile.ZipFile` 读 `xl/media/*`——比 openpyxl `ws._images` 稳，`z.read` 拿字节，矢量/未知位图 `continue`、超单图上限 `continue`、达数量上限置 `truncated=True` break，`base64.b64encode` 返回不落盘）；新增 3 个 action 分支：`comments`→`{comments,count}`、`images`→`{images,count[,truncated]}`、`read_rich`→`{sheet,rows,row_count,col_count,truncated,comments,images,images_truncated}`；`read_sheet` 改用 `_read_rows`。②**`app/src/lib/agentRunner/types.ts`**：`CollectedInput{text; images:ChatImage[]}`（import `ChatImage`），供全链路替换 `string`。③**`app/src/lib/agentRunner/inputs.ts`**：新增 `pathExt`/`IMAGE_MEDIA_TYPES`/`isImagePath`/`imageMediaType`；`toolForInputPath` 用 `pathExt`（图片仍落 `file`）；`readFileInput`→`readSourceInput(path,signal):Promise<CollectedInput>`——图片走 `file` `mode:'binary_b64'` 读 base64→`{text:'[图片] 名', images:[{mediaType,base64}]}`、Excel 走 `read_rich`（`rowsToText` 正文＋`commentsToText` 批注 `【批注】Sheet!Cell（作者）：…` 拼文本、`xlsxImagesToChatImages` 转图）、docx(`action:read`)/pdf(`action:text`)/纯文本(`action:read,mode:text`) 仍 `{text, images:[]}`；`collectInput` 返 `CollectedInput`，sources/url/history/file 各分支累加 `sections`(join)＋`images`(concat)，新增 `textInput` 助手给纯文本分支（images 空）；`XlsxComment`/`XlsxImage` 接口。④**`app/src/lib/agentRunner/modelCalls.ts`**：`buildUserContent(prompt,images,api)`——无图纯串（保 prompt caching），anthropic `{type:'image',source:{type:'base64',media_type,data}}`＋text、openai/gemini text＋`{type:'image_url',image_url:{url:'data:${mediaType};base64,${base64}'}}`（复用 llmClient 两套结构）；`callNodeModelWithPrompt(node,prompt,signal?,images=[])` 加 **vision 硬拦**（`images.length>0` 且 `config.models.find(m=>m.id===ref.modelId)?.caps?.vision!==true` 抛错，措辞避开「超时/网络/临时」等 `RETRYABLE_NODE_ERROR_PATTERNS` 关键词故不进重试）、`messages:[{role:'user',content:buildUserContent(...)}]`、`system` 仍走顶层字段不动稳定前缀；`callNodeModel(node,input:CollectedInput,signal?)` 透传 `input.images`；`convertInputToSchema` 走默认空 images（纯文本转换不带图，行为不变）。⑤**`app/src/lib/toolRegistry.ts`**：Excel `capabilities` 补「读取单元格批注」「提取内嵌图片」。**门控/数据结构不动**：图片复用 `dataSourceFiles`＋扩展名识别（不加 `dataSourceImages`），图片走 `file` 工具故用户仍勾 `file` 标签、Excel 抽图在 Python 内部完成故 excel 节点门控仍只需 `excel`，画布/Agent 导出导入无需改。**无 Python LLM 层改动**（`llm_calling.py` `messages` 原样透传，图进文本出，回复仍取 `str`）、无新依赖、无 Rust 改动；只改 `python/tools/`，`src-tauri/python-dist/` 是 build 产物、发版再跑 build-dist。已通过 `python -m py_compile tools/excel_tool.py`、`npx tsc -b`、`npx oxlint`。剩余端到端手测待用户（桌面 tauri dev，需给某模型勾 vision 或换真·多模态模型）：**通用图片**——节点数据源选 png、勾 `file`、绑视觉模型→运行问「描述这张图」模型能答出内容；**复杂 Excel**——含内嵌示意图＋单元格批注＋正文的 .xlsx 作数据源、绑视觉模型→输出体现对示意图/批注/正文的理解；**硬拦**——同一带图节点绑非 vision 模型→运行时明确报错「未标记支持视觉」不静默丢图不进重试；**回归**——纯文本/docx/pdf 节点（无图）行为与改造前一致走纯串 content。**自检自修**：独立审视发现 `read_rich` 的 comments 与 rows 不对齐——`sheet_name=None` 时 rows 取 `wb.active`、但 `_extract_comments(wb,None)` 扫**全部** worksheet 的批注，text 会混入其他表的批注却无对应 rows；且 `_extract_comments` 用 `ws.iter_rows()` 不带 `max_row`，对超大 Excel 全表遍历，与 `_read_rows` 的 `limit_rows=10000` 护栏不一致。修法：`_extract_comments` 加 `limit_rows` 参数（`ws.iter_rows(max_row=...)`），`read_rich` 改传 `ws.title`（与 rows 同表）+ `limit_rows`，`comments` action 也加 `limit_rows`（默认 10000）。再核 vision 硬拦措辞「未标记支持视觉」不命中 `RETRYABLE_NODE_ERROR_PATTERNS` 任何关键词（timeout/timed out/network/429/llm 连接中途断开/返回空内容/response ended prematurely/socket hang up）→ 不可重试 OK；`buildUserContent` 两套结构与 llmClient 范本一致；`isImagePath` 大写扩展名经 `pathExt` `toLowerCase()` 归一。py_compile＋tsc＋oxlint 复验通过。

> v2.95 施工同步：门控节点 OR/AND/NOR 控制流（用户要新节点类型做控制流路由：多个上游连到一个中介节点，按「OR=任一通过」「AND=全通过」「NOR=全不通过」决定下游是否执行）。AskUserQuestion 拍板：①`type='gate'`+`data.gateType` 区分；②「通过」=success，skipped 算不通过；③NOT 语义=NOR（「都不通过则执行」=全部非 success→通过，单上游退化为 NOT）；④门控聚合输出透传给下游；⑤不通过=skipped 非 failed；⑥门控不调 LLM/工具/无数据源/不写产物。**施工步骤**：①**`stores/canvas/types.ts`** `AgentNodeData` 加 `gateType?:'or'|'and'|'nor'`；②**`lib/agentRunner.ts`**（核心）——抽 `isGate(id)`/`isReady(childId)`（门控 ready=所有上游 settled[success/failed/skipped]，agent ready=所有上游 success[原逻辑]）、`readyHas` Set 防重复 push、`onNodeSettled(node)`（success/failed/skipped 都推进下游 ready，替代 `onNodeSuccess`）；初始扫描与 onNodeSettled 都用 isReady；`markSkipped` 标自己 skipped 后调 onNodeSettled（skipped 算 settled，可能让 NOR 下游 ready）；`executeNode` 顶部加门控分支（在 `ensureNodeCapabilities` 之前）：算 `successCount` 与 `passed`（or=≥1/and=全/nor=0），通过则聚合上游 `NodeOutput`（每源 content 截断 8000 字符、总封顶 8 源防 prompt 爆）成 `{label,content,summary,nodeId}`、自身 success+onNodeSettled，不通过则自身 skipped+markSkipped 下游；catch 分支加 `onNodeSettled(node)`（失败也算 settled，可能让 NOR 下游 ready）；门控分支 `return` 走 finally `running--` 不死锁；③**`lib/agentRunner/inputs.ts`** 预检四函数短路——`ensureRequiredTools` 循环内 `if(gateType) continue`、`requiredToolTagsForNode` 顶部 `if(gateType) return []`、`ensureNodeCapabilities` 顶部 `if(gateType) return`、`ensureDataSources` 循环内门控节点要求「至少一个上游连接」而非「数据源」；④**`lib/agentRunner/outputFormats.ts`** `outputFormatForNode` 顶部 `if(gateType) return 'markdown'`（占位永不真用）；**`outputWriter.ts`** `persistOutput` 顶部双保险短路（门控分支已在 executeNode return，万一走到直接返回不写产物）；⑤**`components/GateNode.tsx`**（新建）菱形+三色（or 蓝/and 绿/nor 紫）+`∨/∧/⊽` 图标+RunStatusPill 复用（skipped 显示「未通过」、success 显示「通过」）；`CanvasArea.tsx` `nodeTypes` 加 `gate: GateNode`；`CommandPalette.tsx` 加三个命令（OR/AND/NOR 门，`addGateNode(gateType)` 造 `type:'gate'`+`data:{gateType,label}`）；`PropertiesPanel.tsx` 选中 gate 时早期 return 只渲染 label/description/gateType（Segmented 选 or/and/nor），隐藏工具/模型/数据源/输出格式/Schema；`App.css` 加 `.gate-node` 三色+菱形+搜索高亮样式；⑥**`lib/canvasTransfer.ts`** `ExportNodeData` 加 `gateType?`、`exportNodeData` 透传、`parseCanvasImport` 加 `normalizeGateType` 归一（非法值落 undefined）+ 门控节点清掉 toolTags/systemPrompt/schema/outputFormat 防脏数据。**复用** `collectInput`（零改动，门控聚合 content 经现有 sources 分支自然被下游拿到）、`agentTransfer`（不含画布节点）、`NodeOutput`、`markSkipped`（递归 skipped 逻辑不变，只是内部加 onNodeSettled 推进）、`toolRegistry`（门控不声明 toolTags）。**风险控制**：ready 判定是核心复杂度——`markSkipped` 内部调 `onNodeSettled` 可能重复 push，靠 `readyHas` Set 防重；NOR×skipped 传播（上游全 skipped→NOR 通过→下游执行）符合预期；拓扑环现有 topoSort 已拒；聚合 content 每源 8000 字符+总 8 源封顶防 prompt 爆；MAX_PARALLEL_NODES 门控不占并发槽但 `running++` 走 finally 正常减；zustand selector 稳定性不新增 selector。纯前端改动，无 Python/Rust 变更，不跑 py_compile/cargo check。已通过 `npx tsc -b`、`npx oxlint`。剩余端到端手测待用户（桌面 tauri dev）：**OR 通过**（A 成功 B 失败→OR→下游执行，聚合 content 含 A）；**OR 不通过**（都失败→OR skipped→下游 skipped）；**AND 通过**（都成功→AND→下游）；**AND 不通过**（A 成功 B 失败→AND skipped[非 failed]→下游 skipped）；**NOR 通过/兜底**（都失败→NOR→下游执行）；**NOR 不通过**（A 成功→NOR skipped→下游 skipped）；**回归**（纯 agent 画布无门控→行为不变）；**导入导出**（含门控画布导出→导入→gateType 与 type 正确保留）。**后续微调**：①命名英文→中文「或门/与门/非门」（内部 gateType 值不变）；②UI 重做（原菱形+文字符号偏丑→圆角矩形+左侧彩色竖条+SVG 门控符号+浅 tint 底色）；③**非门暂屏蔽**（用户语义未明确，仅从 CommandPalette 移除非门入口，编排器/GateNode/属性面板/导入导出的 nor 支持代码全保留，语义明确后加回入口即可）。

> v2.96 施工同步：报告中心 Token 用量统计（用户要在报告中心加 token 用量统计：只统计用量不计费、永久累计持久化，三维度——① 按模型 total ② 姬子总控 ③ 每节点+总计，已删除节点汇总一行）。**根因**：两条 LLM 链路都把厂商响应的 `usage` 丢了——节点走 Python `llm_calling.py`（只返回 reply/model）、姬子走 `llmClient.ts chat()`（只返回文本）。**关键设计**：两链路各有唯一 LLM 入口（节点 `callNodeModelWithPrompt`、姬子 `chat()`），在入口内部记录 token 到统计 store、**返回类型保持不变**，故 agentRunner/inputs/masterPlanner/orchestrator/toolGenerator 等调用方零改动。**施工步骤**：①**新建 `app/src/stores/tokenStatsStore.ts`**（仿 uiStore persist 范式）：state `byModel: Record<string,number>` / `byNode: Record<string,{label,total}>` / `masterTotal` / `grandTotal`；action `recordNode(nodeId,label,model,total)`（byNode 累加+更新 label、byModel 累加、grandTotal 累加）/`recordMaster(model,total)`（masterTotal+byModel+grandTotal 累加）/`reset()`；`total<=0` 静默跳过；persist `name:'multi-agent-token-stats'`+`createProjectStorage()`+partialize 只留 4 数据字段。②**`python/tools/llm_calling.py`**：`_call_openai` 解析后 `usage.get("total_tokens")`、`_call_anthropic` `input_tokens+output_tokens`，两者签名改 `-> tuple[str,int]` 返回 `(content,total)`；`execute` 接元组 `reply, total_tokens = await asyncio.to_thread(...)`，返回加 `"usage": {"total": total_tokens}`；usage 缺失兜底 0 不报错。③**`app/src/lib/agentRunner/modelCalls.ts`**：`callNodeModelWithPrompt` 的 `unwrapToolResult` 泛型加 `usage?:{total?}`，取 reply 后 `total>0` 则 `useTokenStatsStore.getState().recordNode(node.id, nodeLabel(node), ref.modelId, total)`（主调用+schema 转换都经此函数，覆盖节点所有 LLM 消耗），返回类型仍 `string`；import useTokenStatsStore。④**`app/src/lib/llmClient.ts`**：`chat()` anthropic 分支解析 `data.usage.input_tokens+output_tokens`、openai 分支 `data.usage.total_tokens`，经私有 `recordChatUsage(model,total)`→`recordMaster`（加注释：chat 现所有调用点[masterPlanner/orchestratorStore/toolGenerator]都是姬子侧，计入总控正确，未来非姬子场景需另分流），返回类型仍 `Promise<string>`；import useTokenStatsStore。⑤**`app/src/components/ReportCenter/metrics.ts`**：加 `TokenMetrics`/`TokenModelRow`/`TokenNodeRow` 接口 + `buildTokenMetrics(stats, existingNodeIds)`（遍历 byNode：nodeId 在 existingNodeIds→活跃节点行，否则累加 deletedTotal；byModel/activeNodes 按 total 降序）+ `formatTokens`（千分位 `toLocaleString('en-US')`）；**`ReportCenter.tsx`**：`useTokenStatsStore` 订阅 byModel/byNode/masterTotal/grandTotal + `useCanvasStore` 的 canvases/savedCanvases，useMemo 交叉所有节点 id 算 tokenMetrics；统计卡区下方加 `<section className="report-tokens">`（总计/总控两卡 + 按模型表 + 按节点表[活跃节点行 + deletedTotal>0 时「已删除节点」行] + 「清零统计」按钮走 `modal.confirm` 二次确认→`reset()`），grandTotal===0 时 Empty 占位；`App.css` 加 `.report-tokens`/`.report-token-grid`/`.report-token-block`/`.report-token-row` 样式。**复用**（不改）：`lib/tauriStorage.ts`（createProjectStorage）、`uiStore`（persist 范式）、`canvasStore`（canvases/savedCanvases 判节点存在）、`agentRunner.ts`/`inputs.ts`/`types.ts`（入口函数内部记录，返回类型不变故不动）。**风险控制**：usage 缺失/流式无 usage→total=0 静默不记录（不报错不污染统计）；只记 total（anthropic input+output、openai total_tokens 语义对齐）；节点存在性冷启动时 canvases 可能空、以 savedCanvases 为准（未保存画布节点暂算已删除，可接受近似）；selector 稳定性——交叉计算放 useMemo 不在 selector 现算返回新对象；新增 `data/multi-agent-token-stats.json` 只有 token 数字无敏感数据无外发面；不改 Python LLM 主链路行为（只多带 usage，reply 解析/重试/超时/多模态不变）。已通过 `python -m py_compile tools/llm_calling.py`、`npx tsc -b`、`npx oxlint`。剩余端到端手测待用户（桌面 tauri dev）：跑含 2-3 节点画布→报告中心出现各节点 token+按模型+总计；和姬子对话几轮→总控累计增长；删掉跑过的节点→其 token 归入「已删除节点」行；点清零→归零，重启应用→统计仍在（永久累计验证）；回归正常运行/姬子对话不受影响。

> v2.97 施工同步：两项调试体验修复。**①姬子创建指令卡顿**（用户反馈发创建节点/画布消息会先卡十几秒才弹确认、消息不先显示、无思考提示，不丝滑）。根因：`components/MasterAgentPanel.tsx` `onSend` 的规划分支顺序反了——先 `await planMasterActionWithLLM(...)`（十几秒 LLM 调用）、最后才 `addMessage` 用户消息；而聊天分支（用户消息+sending 占位在 await 前）与造工具分支都是正确顺序。施工：在 `if(attachments.length===0)` 块前声明 `let planUserMsgAdded=false; let planAssistantId: string|undefined;`（跨块，供聊天回落复用）；规划分支进入后、await 前先 `addMessage({role:'user',content:text,status:'done'})`+`setDraft('')`+`planAssistantId=addMessage({role:'assistant',content:'',status:'sending'})`；action 命中时用 `updateMessage(planAssistantId,{content:notice,status:'done'})` 复用占位（planAssistantId 为空才走旧 addMessage 双条路径，兼容无模型走规则兜底场景）；聊天回落路径把用户消息 `addMessage` 包 `if(!planUserMsgAdded)`、assistant 占位改 `planAssistantId ?? addMessage(...)`。副作用修复：规划期间现在有 sending 占位→`activeSending` 生效，杜绝重复点发送触发多次规划。效果：发送→消息立刻出现→姬子转「思考中」→弹确认。**②每次调试启动白屏一段时间**。根因：`app/vite.config.ts` 无 `optimizeDeps.include`，dev 模式 Vite 首次加载途中「发现」新依赖会触发重新预打包+整页重载（日志 `Re-optimizing dependencies because vite config has changed`），表现为白屏。施工：`optimizeDeps.include` 显式列出全部一级重量级依赖（react/react-dom/react-dom/client/react/jsx-runtime/zustand/zustand/middleware/antd/antd/locale/zh_CN/@ant-design/icons/@xyflow/react/@tauri-apps/api/@tauri-apps/api/core/plugin-dialog/plugin-http/plugin-opener），使预打包确定化、避免加载途中二次优化重载。**性质**：仅影响 dev 调试体验（Vite 按需编译固有代价），正式 NSIS 安装包是预打包静态文件秒开、完全不受此配置影响；改 vite.config 需重启 dev server 生效。均纯前端改动，无 Python/Rust 变更，已通过 `npx tsc -b`、`npx oxlint`。手测：给姬子发创建画布指令→消息立刻显示+思考态+弹确认；重启调试观察白屏时长缩短。

> v2.98 施工同步：外部审计 9 项缺陷核实与修复（用户拿一张 9 项缺陷表要求逐条对照真实代码核实真伪）。**核实结论**：9 条全属实（#9「工程边界混乱」半真——根目录 data/logs/outputs 混源码是绿色便携架构刻意设计），其中 #1/#6 是近期改动直接引入/遗漏的真 bug。按「不过度架构化、安全边界靠服务端硬拦不靠前端约定」分两批修。**第一批(低成本+功能 bug)**：①**#1 Python 版本号**——加 Token usage 返回却没升 `LLM_CALLING_VERSION`(旧串)、前端 `EXPECTED_PYTHON_SERVICE_VERSION` 也旧串→`ensureCompatiblePythonService` 认不出旧后台不强制重启→Token 恒 0。两处同步升 `2026-07-08.token-usage`+两处加「改工具返回结构/行为必须同步升版本」强注释+错误总结 checklist 补两条(版本同步、安全边界)。②**#6 门控失败传播**——`markSkipped`(agentRunner.ts) 无差别把门控节点当普通下游递归跳过：A/B→OR 门→C，A 先失败即把 OR 门标 skipped，B 成功也白搭→兜底不跑(「明明有兜底为何不跑」)。修：`markSkipped` 顶部 `if(isGate(nodeId)) return`——门控不被上游 failed/skipped 被动跳过，保持 queued，靠各上游 settle 时的 onNodeSettled 在最后一个上游 settled 后 push 进 ready→executeNode 门控分支自评估。failed 链(catch 先 markSkipped 后 onNodeSettled)与 skipped 链(markSkipped 内先 onNodeSettled 后递归)都覆盖，无死锁。③**#7 非原子写**——`storage.rs` `storage_set` 由 `fs::write(&path,value)` 直接覆写改：写前若目标存在 `fs::copy` 一份覆盖式 `.bak`(单份不堆积、失败不阻断)→写同目录 `.tmp`→`fs::rename` 原子替换(同卷 rename 原子，rename 失败清 tmp)。`resolve` 返回 `<key>.json`、key 不含点，`with_extension` 干净替换。④**#2 Token 文案**——`ReportCenter.tsx` Token 区加 `.report-tokens__disclaimer` 免责小字「仅供参考不作计费依据，部分模型/中转不返回用量、失败重试可能漏记」，不做覆盖率统计。**第二批(安全边界)**：⑤**#4 Python 服务鉴权**——`python_manager.rs` 加 `static SERVICE_TOKEN: OnceLock<String>` + `service_token()`(RandomState OS 熵种子×2 段拼 128bit 十六进制，无需引 rand/uuid)，每 app 会话固定一次、跨 Python 重启沿用；spawn 时 `cmd.env("MULTIAGENT_SERVICE_TOKEN", service_token())`；新增 `service_token_cmd` 命令注册进 `lib.rs`；`app.py` `import os` 读 `SERVICE_TOKEN`，加 `require_service_token` 中间件校验 `x-service-token`(/health 与 OPTIONS 豁免；token 空时不启用兼容手动/dev/浏览器预览)，CORS `allow_headers` 补 `X-Service-Token`；`pythonClient.ts` 加 `cachedServiceToken`+`getServiceToken`(invoke service_token_cmd 缓存)+`pyFetch`(注入头，401 清缓存重取重试一次)，listTools/listToolMeta/installTool/removeTool/executeTool 5 端点全改走 pyFetch，getHealth 保持豁免。核实全项目仅 pythonClient.ts 直连 18081，无绕过。⑥**#5 装工具执行顶层代码**——硬门放 `installer.py`(非前端，防直接打 /tools/install 绕过)：新增 `_is_main_guard` + `check_top_level_side_effects(tree)`，`compile_check(code,name,allow_side_effects=False)` 末尾在不放行时调扫描。顶层只允许 Import/ImportFrom/Func/AsyncFunc/Class/Assign/AnnAssign/AugAssign/Pass/docstring-Expr/`__main__` guard；拦裸调用表达式(Expr+Call，结果丢弃=纯副作用)、顶层 For/While/With/Try/非守卫 If。低误报(放行 `logger=getLogger`/`_RE=re.compile` 赋值)，已知局限 `_=os.system(...)` 赋值型不拦、依赖人工审阅兜底(纵深防御非唯一防线)。`install_tool` 读 `allow_top_level_side_effects`(默认 False)传入；`pythonClient.ts` `InstallToolPayload` 加可选同名字段(plumbing 就绪，UI 勾选开关后续加)。#3/#8/#9 缓做。均已通过 `python -m py_compile app.py tools/installer.py tools/llm_calling.py`、`npx tsc -b`、`npx oxlint`、`cargo check --offline`。剩余手测待用户：门控六场景(尤其 A 失败 B 成功→OR 通过、全失败→NOR 兜底跑)、Token 统计随运行/姬子对话增长且免责文案在、装工具带顶层副作用被拒/放行、鉴权(伪造请求被 401)、原子写(跑一轮看 data/*.bak 生成)。

> v2.99 施工同步：运行历史状态视觉优化（用户反馈①运行中的历史项也要有转圈态、跑完恢复时钟；②状态标识符号颜色太近似——原图标恒灰 `#86909c` 不随状态变、左侧竖条几种莫兰迪暖色[绿#6da58a/金#c2a55f/玫#c86b6b]明度饱和度太接近）。施工：①`components/LeftSidebar/RunHistory.tsx` import `LoadingOutlined`，按 `r.runState?.status==='running'` 渲染 `<LoadingOutlined spin>` 否则 `<ClockCircleOutlined>`，图标 className 加 `history-item__icon--${status.tone}` 按状态上色；②`App.css` 新增 `.history-item__icon--{success/failed/running/cancelled/neutral}` 图标色 + 重配 `.history-item__rail--*` 竖条色为色相拉开的莫兰迪：成功=沉绿 `#4f9e77` / 失败=陶土红 `#c4574f` / 运行=赭橙 `#d78b3c`(原金与绿太近改偏橙) / 中止=雾紫 `#9a7fb0`(从原与 neutral 共用灰独立) / 未运行=灰 `#aeb4bd`；`cancelled` 与 `neutral` 拆开各自定义。图标不再恒灰、符号本身即可辨状态。纯前端、无 Python/Rust 改动、Vite 热更新即生效，已通过 `npx tsc -b`、`npx oxlint`。手测：跑画布时左侧运行历史该条转圈、跑完变时钟，success/failed/running/cancelled 四态颜色区分明显。

> v3.0 施工同步：审计二轮 review 的两处修复 + 前端测试基建。**①放行口修复(#1 的真正落地)**：v2.98 时 `installer.py` 与前端 `pythonClient.ts` 都加了 `allow_top_level_side_effects`，但 `python/tools/router.py` 的 `InstallRequest`(Pydantic BaseModel) 未声明该字段——`install_tool` 入口 `body.model_dump()` 只输出已声明字段，导致放行位被静默丢弃、installer 永远 `allow_side_effects=False`，「人工放行」实际不可用(默认硬拒绝仍生效)。施工：`InstallRequest` 补 `allow_top_level_side_effects: bool = False`(带注释说明「必须声明否则 model_dump 丢弃」)，放行口贯通。并在 `python/tests/test_installer.py` 新增 `TopLevelSideEffectTests`(6 用例)：`_code()` 辅助拼合法工具+给定顶层 body，覆盖 允许 import/常量/logger=getLogger/re.compile 赋值、允许 `__main__` guard、拒绝裸调用(os.system/print/os.remove)、拒绝顶层 for/while/with/try、allow_side_effects=True 时放行、无 execute 即使放行仍拒。`python -m pytest tests/test_installer.py` 22 passed。**②门控自动化测试(方案 A,引入 vitest)**：门控是功能正确性 bug 却只有手测,用户选 A。前端此前无测试框架,`npm install -D vitest`(联网,+29 包)。抽门控纯判定逻辑到 `app/src/lib/agentRunner/gateLogic.ts`——`isNodeReady(gateType, parentStatuses)`(门控要求全 settled、agent 要求全 success)+`gatePassed(gateType, parentStatuses)`(or≥1/and 全/nor=0 success);`agentRunner.ts` 的 `isReady` 与 executeNode 门控分支 `passed` 改为复用这两个纯函数(保证「测的即用的」,successCount 仅留作状态文案)。新增 `agentRunner/gateLogic.test.ts` 7 用例:isNodeReady 的 agent/门控/空上游 + gatePassed 的 OR/AND/NOR + skipped 语义,重点锁定「A 失败 B 成功→OR 通过」「全失败→NOR 兜底跑」不回归。`package.json` scripts 加 `"test": "vitest run"` + devDep vitest。`npx vitest run` 7 passed。**其余 review 点(记 backlog)**：赋值型 `_=os.system()` 顶层副作用不拦(纵深防御非唯一防线,需递归查赋值右侧危险调用才能升级)、token 用 RandomState+时间+pid 非密码学安全随机(分发前换 getrandom/rand)、空 token 不鉴权(仅 Tauri 拉起的 Python 有保护,兼容手动/dev 的取舍)。均纯代码改动无 Rust 变更,已通过 vitest(7)+installer unittest(22)+`npx tsc -b`+`npx oxlint`+`python -m py_compile tools/router.py`。

> v3.01 施工同步：审计三轮 review 的两处遗漏修复。**①打包用 python-dist 仍是旧代码**——v3.0 改了源码 python/ 的版本号/鉴权/AST 硬门,但 `app/src-tauri/python-dist/`(发版打进 NSIS 的)没同步,仍是 `2026-07-05.audit-ssrf-uac-async`、无 token 鉴权、无新版安装器安全门。后果:开发正常但一打安装包就带旧 Python,前端「版本不匹配」反复重启仍不匹配,安全修复也不进包。施工:`python/build-dist.mjs` 加 `--sync-only` 参数分支——只跑 `copyAppCode()`(把 app.py+tools/ 拷进已存在的 python-dist),跳过 CPython 下载/pip 装(保留 runtime/ 与 site-packages/),`main()` 顶部 `const syncOnly = args.has('--sync-only')` 早 return;用法 `node python/build-dist.mjs --sync-only`。跑后验证:`python-dist/tools/llm_calling.py` 版本号=`2026-07-08.token-usage` ✓、`python-dist/app.py` 有 SERVICE_TOKEN×4 ✓、`python-dist/tools/installer.py` 有 _ASSIGN_SAFE_CALLS×5 ✓、`python-dist/tools/router.py` 有放行位字段 ✓。**②赋值绕过**——实测 `_ = os.system('echo hi')` 通过 compile_check(此前 `check_top_level_side_effects` 仅拦裸调用 Expr+Call,Assign 右侧 Call 漏拦,我 v2.98 注释里也写了「已知局限」但没修)。施工:`installer.py` 升级扫描——顶层 `Assign/AnnAssign/AugAssign` 右侧用 `ast.walk` 递归找所有 Call 节点,每个 Call 必须命中新增白名单 `_ASSIGN_SAFE_CALLS`(logging.getLogger/re.compile/collections.namedtuple/dataclasses.dataclass/enum.Enum/pathlib.Path/decimal.Decimal/datetime.{datetime,date,time,timedelta})否则报问题行;匹配走 `_call_qualname` 算 `module.func` 全限定 或 单 `func` 名两套(兼容 `getLogger(__name__)` 这种未限定的)。错误消息从「顶层可执行语句」改「顶层可执行副作用」并列具体问题行+调用名。`_=os.system`/`data=requests.get(...)`/`x=subprocess.run(...)`/`ret:int=os.system(...)`(AnnAssign)/嵌套 `re.compile(os.system(...))` 全部拦下。`tests/test_installer.py` `TopLevelSideEffectTests` 新增 4 用例(test_rejects_assignment_side_effect 含 4 种赋值形态/test_allows_safe_assignment_calls_only/test_rejects_nested_dangerous_in_safe_wrapper/test_allow_flag_overrides_assignment),错误消息正则从「顶层可执行语句」同步改「顶层可执行副作用」,共 26 passed。已通过 `python -m py_compile`+installer unittest(26)+`npx tsc -b`+`npx oxlint`+`npx vitest run`(7)。

> v3.02 施工同步：姬子能力与 UI 分层修正：修复已有模型能力自动识别不生效（modelStore 持久化版本升到 2，旧模型配置会重新合并推断能力；MiMo v2.5/omni/pro 规则补齐，避免 pro 被误标多模态）；外部姬子状态栏瘦身为后台/模型/工具/待处理/问题/画布诊断，任务计划与一键体检移入姬子抽屉输入区；Skill 能力商店取消“自建”页签，改按 Skill 名称、描述、能力、说明自动分到工作流/工具/诊断/模型；同步短标签与紧凑布局 CSS。验证：npm.cmd run lint 通过，npm.cmd run test -- --run 通过（28 passed），npm.cmd run build 通过；浏览器插件验证连续超时，未作为通过依据。

---

## 施工原则

1. **先基础后功能**：先搭建框架，再实现功能
2. **先单点后系统**：先实现单个组件，再集成系统
3. **先核心后边缘**：先实现核心功能，再完善边缘功能
4. **先本地后集成**：先本地调通，再集成外部服务

---

## 施工顺序

### 阶段 1：项目基础搭建（Day 1-2）

#### 1.1 初始化项目结构
- [x] 创建项目根目录（`app/`）
- [x] 初始化 React + Vite + TypeScript 项目
- [x] 安装核心依赖：
  - [x] `@xyflow/react`（画布组件）
  - [x] `antd`（UI 组件库）
  - [x] `zustand`（状态管理）
  - [x] `@tauri-apps/cli`（桌面框架）*（已接入并起桌面窗口，`cargo build`/`tauri dev` 验证通过）*

#### 1.2 搭建基础目录结构
```
multi-agent-tool/
├── src/
│   ├── components/          # React 组件
│   │   ├── AgentSidebar/    # Agent 侧边栏
│   │   ├── Canvas/          # 画布组件
│   │   ├── PropertiesPanel/ # 属性面板
│   │   └── MasterAgent/     # 总 Agent 面板
│   ├── stores/              # Zustand 状态管理
│   ├── services/            # 服务层（API 调用）
│   ├── types/               # TypeScript 类型定义
│   └── utils/               # 工具函数
├── python/                  # Python 服务
│   ├── app.py              # FastAPI 主应用
│   ├── tools/              # 工具实现
│   └── requirements.txt    # Python 依赖
└── public/                  # 静态资源
```

#### 1.3 配置开发环境
- [x] 配置 Vite（代理、别名、热更新）
- [x] 配置 TypeScript（严格模式、路径别名）
- [x] 配置 ESLint + Prettier（代码规范）
- [x] 测试开发服务器启动（`npm run dev` / `tauri:dev`，开发窗口使用 http://127.0.0.1:5173）

**验收标准**：
- ✅ 项目可以正常启动
- ✅ 开发服务器运行在 `http://127.0.0.1:5173`
- ✅ 无 TypeScript 错误

---

### 阶段 2：UI 框架搭建（Day 3-4）

#### 2.1 实现主界面布局
- [x] 创建 `App.tsx` 主布局
- [x] 实现三栏布局：
  - [x] 左侧：Agent 侧边栏（280px，支持拖拽调整宽度）
  - [x] 中间：画布区域（flex: 1）
  - [x] 右侧：属性面板（300px，当前为空占位，支持拖拽调整宽度）
  - [x] 顶部：总 Agent 面板（几经迭代：悬浮窗 FAB → 右侧栏页签 → 现为顶部抽屉，pill 触发条展开/收起，主界面整体下沉）
- [x] 左右侧栏支持拖拽调整宽度（内侧手柄拖拽，限定最小/最大范围，持久化保存于 uiStore）
- [x] 全局渲染错误边界（`ErrorBoundary.tsx` class 组件在 `main.tsx` 根部包裹整个应用，未处理的 React 渲染异常展示「界面出错了」结果页 + 重载按钮，`console.error` 记录 componentStack，避免整屏白屏）

#### 2.2 实现 UI 设计系统
- [x] 配置 Ant Design 主题（浅色模式）
- [x] 定义全局样式：
  - [x] 圆角（8px-12px）
  - [x] 阴影（`box-shadow: 0 2px 8px rgba(0,0,0,0.1)`）
  - [x] 配色（主色 #1890FF）
  - [x] 字体（Segoe UI / 系统默认）

#### 2.3 实现基础组件
- [x] 按钮组件（复用 antd Button）
- [x] 输入框组件（复用 antd Input，带聚焦效果）
- [x] 卡片组件（Agent 卡片/实例卡片，带阴影和圆角）

**验收标准**：
- ✅ 主界面布局正确显示
- ✅ UI 符合现代简约风格
- ✅ 响应式布局正常

---

### 阶段 3：画布系统（Day 5-7）

#### 3.1 实现画布基础功能
- [x] 集成 React Flow
- [x] 实现画布创建、保存、加载
- [x] 实现多画布管理（最多 20 个）
- [x] 画布数据持久化（Zustand persist，落地项目内明文 JSON `data/multi-agent-canvas.json`，桌面端经 Rust 命令读写，纯浏览器回落 localStorage）

#### 3.2 实现节点系统
- [x] 实现 Agent 节点组件
- [x] 实现节点拖拽（从侧边栏到画布）
- [x] 实现节点移动、删除
- [x] 实现节点状态显示（等待/运行中/成功/失败/跳过）*（由 Agent Runner 写入 `runState`，节点卡片显示状态胶囊）*

#### 3.3 实现连线系统
- [x] 实现连线创建（从输出端口到输入端口）
- [x] 实现连线删除
- [x] 实现连线样式（带箭头的曲线）
- [x] 实现连线选中态视觉区分（默认蓝 `#1890ff`、选中变橙 `#fa8c16` 加粗 3px、悬停淡橙，CSS `!important` 覆盖内联 stroke；箭头同步变色）

#### 3.4 实现画布操作
- [x] 实现画布缩放（滑块 + Controls，65%–200% + 适合屏幕）
- [x] 实现画布平移
- [x] 实现撤销（Ctrl + Z，session 内 50 步栈；重做按需求暂不做）
- [x] 实现节点复制/粘贴（Ctrl + C / Ctrl + V，含两端在选区内的连线）
- [x] 实现框选多选（按住 Ctrl 空白拖动，多选整体移动/删除）
- [x] 实现子节点折叠（父节点递归折叠下游后代 + 一键全部折叠/展开，视觉隐藏、数据保留）
- [x] 实现节点搜索（Ctrl+F 拦截浏览器原生查找条，仅匹配节点名称，命中黄框+当前项橙框高亮+平移定位，经派生 `displayNodes` 注入不写 store）

**验收标准**：
- ✅ 可以创建多个画布
- ✅ 可以拖拽节点到画布
- ✅ 可以连接节点
- ✅ 可以撤销/重做操作
- ✅ 画布数据正确保存和加载

---

### 阶段 4：Agent 系统（Day 8-10）

#### 4.1 实现 Agent 配置
- [x] 实现 Agent 配置界面*（`AgentConfigModal.tsx`；两层属性模型——库编辑「定义」，拖入画布快照复制为独立「实例」，两者解耦）*
  - 名称、描述
  - 系统提示词
  - 工具标签选择
  - LLM 选择（下拉框）
  - 输入/输出 schema 定义*（v2.80 已移除编辑 UI：数据字段与 Runner 校验/匹配逻辑保留，但按用户反馈不再暴露手动输入框——自动双产物流转已满足需求）*

#### 4.2 实现 Agent 管理
- [x] 实现 Agent 创建*（新建下拉选预置模板/空白，落库后直接打开编辑）*
- [x] 实现 Agent 克隆*（`cloneAgent`，库卡片 hover 显现克隆图标，名称加「副本」）*
- [x] 实现 Agent 删除（检查画布引用）*（`countAgentRefs` 扫描打开画布+已保存副本节点，删除前提示引用数）*
- [x] 实现 Agent 导出*（`agentTransfer.ts` 构建可移植信封：剥离 id/时间戳、modelRef 置空只留 modelId 提示；save 对话框选路径→Python file-write 落盘；库卡片新增导出图标）*
- [x] 实现 Agent 导入*（库顶部「导入」按钮＋隐藏 file input 选 `.agent.json`，`fileToText` 读取（不走沙箱）→`parseAgentImport` 校验 kind/schema＋防御性取字段→过滤本机不存在的工具标签（剔除并提示）→`addAgent` 落库后 `openEdit` 打开编辑器补模型；modelRef 置空、modelHint 提示重选）*

#### 4.3 创建预置 Agent 模板
- [x] 需求分析师
- [x] 测试用例生成器
- [x] Bug 报告生成器

#### 4.4 实现 Agent 存储
- [x] Agent 配置持久化*（实际机制：非按 Agent 拆目录，而是单一 `data/multi-agent-agents.json` 存全部 Agent 定义，`agentStore` persist）*
- [ ] 报告模板持久化（`agents/{id}/template.md`）*（模板渲染机制未做）*
- [x] 添加 version 字段*（`AgentDef.version` 字段已存在）*

**验收标准**：
- ✅ 可以创建、编辑、删除 Agent
- ✅ 可以克隆 Agent
- ✅ 可以导出/导入 Agent
- ✅ 预置 Agent 模板可用
- ✅ Agent 数据正确保存

---

### 阶段 5：Python 服务搭建（Day 11-12）

#### 5.1 搭建 FastAPI 服务
- [x] 创建 Python 项目结构
- [x] 实现 FastAPI 主应用（`python/app.py`）
- [x] 配置 CORS（允许 Tauri / 本地开发前端访问）
- [x] 实现健康检查接口（`GET /health`）

#### 5.2 实现进程管理
- [x] Tauri 启动时自动启动 Python 服务
- [x] 实现服务健康检查（前端 `pythonClient.ts` 轮询 HTTP health + Rust `python_status`）
- [x] 实现服务崩溃自动重启（最多 3 次）
- [x] 应用关闭时停止 Python 服务
- [x] 标题栏提供「重启后台」按钮：停止托管 Python 子进程、清理占用 8080 的旧 uvicorn 后台服务并重新启动，避免前台重启后仍连到旧后台

#### 5.3 实现 Python 环境管理
- [x] 检测 Python 环境
- [x] 自动创建 virtualenv
- [x] 自动安装依赖（`requirements.txt`）
- [x] 环境异常时提示用户
- [x] 当前工作区 `python/venv` 已修复并补齐依赖

**验收标准**：
- ✅ Python 服务代码已完成，当前工作区 venv 可启动并已验证依赖导入
- ✅ 前端访问 Python 服务链路已接入，`/health` 与 `/tools` 已验证
- ✅ 服务崩溃后自动重启已接入 Rust `python_status`
- ✅ Python 环境自动配置脚本已完成

---

### 阶段 6：工具系统（Day 13-15）

#### 6.1 实现工具注册表
- [x] 创建工具注册结构 *（当前为 `tools/router.py` 静态 `_REGISTRY`，不是 `tools.json` 动态注册）*
- [ ] 实现工具注册接口（`POST /tools/register`）*（MVP 暂不需要动态注册）*
- [x] 实现工具查询接口（`GET /tools`）

#### 6.2 实现内置工具
- [x] `file-read`：读取文件
- [x] `file-write`：写入文件
- [x] `excel`：Excel 操作（新建、修改、读写、删除）
- [x] `pdf-read`：读取 PDF
- [x] `llm-calling`：调用 LLM API

#### 6.3 实现工具调用
- [x] 实现工具调用接口（`POST /tools/{tool_name}/execute`）
- [x] 实现工具执行错误处理
- [ ] 实现工具执行日志记录

#### 6.4 实现标签管理
- [x] Agent 创建时选择工具标签
- [x] Agent 运行时加载对应工具 *（轻量装配校验：`ensureRequiredTools` 已把节点 `toolTags` 纳入运行前必备工具集，缺失则开跑前报「Python 工具服务缺少：…」；未引入 LLM function-calling）*
- [x] 工具标签动态修改

**验收标准**：
- ✅ 5 个内置工具代码已实现，工具列表接口已验证
- ✅ 工具 HTTP API 已实现，`file-read` 最小执行调用已验证
- ✅ 工具执行错误分类返回
- ✅ 工具库展示已同步实际状态，已落地工具显示「已落地」
- ✅ 工具库服务状态改为后台轮询，避免每次打开都明显等待检测
- ✅ Agent 运行时按标签装配工具（轻量校验：运行前按节点 `toolTags` 校验 Python 工具可用性）

---

### 阶段 7：Agent Runner（Day 16-18）

#### 7.1 实现单 Agent 执行
- [x] 实现 Agent Runner 核心逻辑（MVP）
  - [x] 读取输入数据（前序输出 / 手动文件输入）
  - [x] 调用 LLM API（经 Python `llm-calling` 工具）
  - [x] 保存输出结果（可选写出 Markdown，并回填 `lastOutput`）
- [x] 实现 LLM 会话隔离（MVP：每个节点每次执行独立 messages）
- [x] 实现执行超时控制（当前工具调用 120 秒）

#### 7.2 实现双格式输出
- [x] 实现 Markdown 输出（MVP：按节点名写出 Markdown）
- [x] 实现 Word/Excel/思维导图输出（`.docx`/`.xlsx`/`.html`，并提供 Word JSON 失败兜底）
- [x] 实现机器可读结构化数据输出（同目录 `data.json` 信封，含画布/节点/摘要/产物路径/结构化数据/原始回复）
- [x] 实现内置输出 schema 校验与自动修复（Word/Excel/思维导图/Markdown）
- [x] 实现 JSON 输出（按 Agent 自定义 schema）*（Agent/节点已支持输出 JSON Schema 文本，Markdown 节点可按自定义 schema 解析为机器可读 JSON 并校验；Word/Excel/思维导图在内置结构上叠加自定义 schema 校验）*（v2.91 决策：此**代码接口保留**，但不再补编辑 UI，也不作待办功能追踪——自动流转 + 内置 schema 校验已覆盖绝大多数场景）*
- [x] 实现输出文件大小限制（10MB）*（`outputWriter.persistOutput` 每个写盘分支及 `data.json` 前用 `assertOutputSize`（`new Blob([text]).size`）校验，超 `OUTPUT_MAX_BYTES=10MB` 抛错中止；该错误不在可重试模式内，不会反复重试）*

#### 7.3 实现输入处理
- [x] 实现文件输入读取（属性面板选择真实路径后 Runner 用 `file-read` 读取）*（v2.91 判定：此即 Phase 2「文件上传」的等价实现——.txt/.md/.docx/.pdf 均可选路径读取作节点输入，无需再单列文件上传功能）*
- [x] 实现历史数据选择 *（节点无前序时数据来源新增「历史产物」模式：属性面板 `listOutputReports()` 列历史 `data.json` 供多选，`collectInput` 用 `readFileInput` 读取选中路径作为输入；`ensureRequiredTools` 一并校验读取工具；连上上游后自动清空 `dataSourceHistoryPaths`）*
- [x] 实现输入数据校验（按 schema）*（下游节点可填写输入 JSON Schema；上游汇总结构不匹配时调用下游节点模型转换）*

#### 7.4 实现执行控制
- [x] 实现执行取消（AbortController）*（标题栏运行中切换为「中止任务」；中止后若已有产物，弹窗选择保留或移除）*
- [x] 实现中止产物处理 *（保留产物不动文件；移除产物调用内部 `file-delete` 删除已知产物文件、清理空文件夹并清空节点 `lastOutput`）*
- [x] 实现 LLM 调用重试 *（`llm-calling` 对瞬断、超时、429/5xx 自动重试；连续节点调用前加入 800ms 间隔，减少第二个节点立刻触发限流/服务忙）*
- [x] 实现执行状态显示 *（节点状态胶囊 + 底部画布状态/完成计数 + 右侧属性面板耗时/错误信息；已补百分比进度条：`CanvasRunState` 增 `skipped` 字段，`CanvasStatusBar` 运行中按 (completed+failed+skipped)/total 渲染 AntD `Progress` 窄条）*
- [x] 实现节点三态耗时样式 *（运行中/成功/失败节点参照示意图显示状态胶囊与「已运行：hh:mm:ss」；运行中实时计时，成功/失败展示总耗时；未运行/等待/跳过兼容原有展示）*
- [x] 修正等待态胶囊颜色 *（等待=黄色系，运行中=蓝色系，便于调试区分）*
- [x] 临时状态调试入口已移除 *（删除 `DebugRunStateControl.tsx`、标题栏引用和对应 CSS）*

**验收标准**：
- ✅ 单个/顺序 Agent MVP 可以执行（构建通过；真实 LLM 调用依赖用户配置与密钥）
- ✅ 多格式输出、内置 schema 校验与 Agent/节点自定义 JSON/schema 输出校验已完成
- ✅ 可以取消执行
- ✅ LLM 调用失败可以重试
- ✅ 执行状态已正确显示（含百分比进度条）

---

### 阶段 8：Workflow Engine（Day 19-21）

#### 8.1 实现多 Agent 编排
- [x] 实现顺序执行（A → B → C）
- [x] 实现并行执行（依赖满足的节点进入并行队列，当前最多 4 个节点并发，启动错峰 80ms）
- [x] 实现多画布并行（最多 6 个纳入批次，当前 6 个画布并发运行）

#### 8.2 实现数据流转
- [x] 实现上游输出 → 下游输入（优先传递上游机器可读 JSON，再附摘要和文本兜底）
- [x] 实现 schema 匹配校验
- [x] 实现 LLM fallback 解析（schema 不匹配时）

#### 8.3 实现错误处理
- [x] 顺序执行失败：标记画布失败，保留节点成功态
- [x] 并行执行失败：只标记失败节点，并只跳过它自己的后续依赖
- [ ] 实现错误日志记录

#### 8.4 实现执行状态管理
- [x] 实现节点状态更新（等待/运行中/成功/失败/跳过）
- [x] 实现画布状态管理（运行中/完成/失败/停止 + 完成计数）
- [x] 实现执行历史保存（运行即录一条 `RunRecord`：深拷贝当刻图快照 nodes/edges，与实时画布解耦，删画布/改画布不影响回看；命名 `画布名_yyyymmddhhmmss`；旧记录 persist v0→v1 迁移补空快照；点击历史项以**只读画布 tab**打开[眼睛图标、不可编辑、属性面板灰化、可缩放平移查看]，与可编辑源画布同屏共存、按 runId 去重；新增运行前置保存流程[空画布提示→未保存强制命名→已保存有改动弹另存为新名/覆盖保存→干净直接运行]）

**验收标准**：
- ✅ 多 Agent 可以按顺序/并行执行
- ✅ 数据可以在 Agent 之间流转
- ✅ 错误正确处理
- ✅ 执行状态正确显示
- ✅ 多画布并行已接入标题栏和总 Agent 自然语言动作

---

### 阶段 9：总 Agent（Day 22-24）

#### 9.1 实现聊天界面
- [x] 实现聊天面板（顶部抽屉式，非底部；pill 触发条展开/收起，主界面整体下沉让出上半屏）
- [x] 实现输入框和发送按钮（含模型选择器、文件上传「+」按钮、Ctrl+V 粘贴图片、联网搜索开关；已接入真实发送 `llmClient.chat()`，Enter 发送/Shift+Enter 换行，发送中切换红色停止按钮）
- [x] 实现消息列表显示（用户/AI 双向气泡、思考中 loading、错误红气泡、参考来源块，对话历史 persist；长对话已接入轻量上下文摘要压缩，旧消息自动沉淀为摘要并在后续 prompt 中注入）
- [x] 实现多会话管理（左侧会话栏：新建/点击切换/双击重命名/删除二次确认「永久销毁」；每会话独立消息流；首条用户消息自动生成标题；persist v1→v2 迁移；多轮对话：发送前抓取当前会话最近 20 轮已完成消息作为 `history` 注入 `chat()`）
- [x] 显示名改为「姬子」（触发条 pill、欢迎语、输入框占位符）；新增「姬子配置」弹窗（会话栏齿轮按钮打开，系统提示词仅支持导入本地文本文件[txt/md/csv/json/log/xml/yaml/yml，超 8000 字截断]或恢复默认，不支持手动编辑；`systemPrompt`/`systemPromptSourceName` 持久化于 `masterAgentStore`，persist v2→v3→v4 迁移）
- [x] 实现操作确认对话框 *（识别到本地操作后先要求回复“确认/取消”，确认后执行）*

#### 9.2 实现记忆系统
- [x] 创建记忆结构 *（`MasterMemory` = profile/preferences/resources 三数组，持久化于 `masterAgentStore` 的 `memory` 字段[存于 `multi-agent-master.json`]，非独立 `memory.json`）*
- [x] 实现记忆存储（用户画像、偏好、资源清单）*（`addMemory(kind, text)` / `removeMemory`；注：自动从对话填充留待后续，当前无手动录入 UI）*
- [x] 实现记忆检索（关键词搜索）*（`searchMemory` 纯函数，命中条目在 `MasterAgentPanel.onSend` 注入 prompt 的 `【已知背景】` 段）*
- [ ] 实现记忆容量管理（上限 10,000 条）*（代码未强制上限，`addMemory` 直接追加；个人使用量级下暂无需要，标注为未实现）*

#### 9.3 实现自然语言解析
- [x] 实现意图识别（双路径：`parseMasterAction` 正则快解析 + `planMasterActionWithLLM` LLM 规划多步 JSON 计划；动作/步骤共 8 类：创建画布、一键建整条工作流画布、重命名当前画布、创建 Agent、加节点、连线、删节点、改节点输出格式[markdown/docx/xlsx/mindmap]、运行当前画布；批量运行打开画布入口已按当前策略移除）
- [x] 实现参数提取（画布名、Agent 名）
- [x] 实现操作执行（调用画布/Agent store 与 Runner API）
- [x] 实现操作确认（修改前询问用户）

#### 9.4 实现工具管理
- [ ] 实现工具下载（从预设仓库）*（v2.73：预设仓库/GitHub 安装未做；姬子自动生成工具已落地，见下）*
- [x] 实现工具手动添加 *（v2.73：手动放置 `tools/custom/{module}.py` + `registry.json` 一条即被服务启动扫描注册；`POST /tools/install` 支持前端/姬子按元数据落盘装依赖动态注册，`ToolConfigModal` 展示自定义分组并支持「试运行」验证与删除；姬子自然语言「写个 XX 工具」→ 受控 prompt 生成工具 JSON → 完整代码人工审阅确认 → 安装。安全底线：生成代码必经人工审阅确认才落盘）*
- [x] 实现工具自动打标签 *（v2.73：生成/安装工具时携带 tags，`useToolTags` 合并内置+自定义标签供 Agent/节点标签选择器；模型在受控 prompt 下自动为工具产出 1~3 个短标签）*

#### 9.5 实现 LLM 配置管理 ✅（已提前完成，作为独立「模型配置中心」）
- [x] 实现 LLM 添加/删除/修改（厂商预置清单 chip 双栏选择，同厂商可多份配置，精选星标置顶）
- [x] 实现 API Key 管理（明文存储于 localStorage `multi-agent-models`）
- [x] 实现网络访问错误提示（通信测试延迟分档信号 + 获取模型列表 CORS 兜底提示）
- [x] 附加：模型开关（绿开/红关）、能力标签（1M/视觉/语音，蓝灰切换）、模型自定义名称、网络层集中于 `llmClient.ts`
- [x] 已修复：`llmClient.ts` 同样接入 `@tauri-apps/plugin-http`（此前普通 `fetch()` 直连各 LLM 厂商在桌面应用中同样受 WebView CORS 限制）+ `capabilities/default.json` 显式 allow 13 家预置厂商域名；自定义厂商（`custom`，任意 baseURL）暂不处理，遇到 CORS 报错需用户提供域名后单独追加

#### 9.6 实现联网搜索配置 ✅（多厂商故障转移 + 即时测试）
- [x] `searchProviders.ts` 预置 Tavily/Brave/Serper 三家（含注册链接、免费额度说明）
- [x] `searchClient.ts` 三家协议适配器归一化为 `SearchResult[]` + `searchWithFailover` 按优先级逐家降级 + `testSearchKey` 单家即时验证（发极简查询返回成功条数或失败原因）
- [x] `searchStore` 多份 Key + 启停 + 上下调序（persist v2，从旧 `serperKey` 自动迁移）
- [x] 标题栏「搜索配置」弹窗：每家一行（优先级序号、上下移按钮、启停开关、Key 输入框、获取 Key 链接、**测试按钮**[loading→成功绿字「Key 有效(返回 N 条)」/失败红字原因]）
- [x] 总 Agent 输入区联网搜索开关：无可用厂商时禁用（灰化 + tooltip）；开关 ON 时发送前检索注入 prompt，AI 回复下方附参考来源
- [x] 已修复：接入 `@tauri-apps/plugin-http`（Rust 侧 `reqwest` 实际发起请求）+ `capabilities/default.json` 显式 allow 三家域名，三家厂商在桌面应用中均可用；此前「接入 Tauri 后端即可绕过 CORS」是错误假设——WebView 里的普通 `fetch()` 本质仍是浏览器请求，同样受 CORS 限制，必须用插件 fetch 才能真正绕过（浏览器纯预览 `npm run dev` 仍有此限制，属已知限制）

**验收标准**：
- ✅ 可以通过自然语言控制画布和 Agent
- ✅ 记忆系统正确工作
- ✅ 操作确认机制正常
- ✅ 工具可以添加和管理
- ✅ LLM 配置可以管理

---

### 阶段 10：报告管理（Day 25）

#### 10.1 实现报告存储
- [x] 实现基于 `outputs/` 分级目录 + `data.json` 信封的报告索引
- [x] 实现报告文件命名规范（沿用 `YYYY-MM/YYYY-MM-DD/画布运行名/节点产物文件夹/主产物 + data.json`）
- [ ] 实现传统 reports/ 和 data/ 分离存储 *（当前方案暂不采用，后续按产品需要再拆）*

#### 10.2 实现报告展示
- [x] 实现报告中心查看历史产物
- [x] 实现报告列表（按时间倒序，支持画布/格式/关键词筛选）
- [x] 实现运行指标统计（运行次数、成功率、产物数、平均耗时、失败/中止、最近产出）
- [x] 实现画布/节点表现排行
- [~] 实现报告预览（Markdown 渲染）*（v2.91 取消：改调系统默认程序打开，不自建预览器）*
- [x] 实现报告打开/定位/删除（系统打开主产物或 data.json，定位文件夹，二次确认删除）
- [~] 实现报告下载 *（v2.91 取消：系统打开即产物本体，无需再做下载）*

#### 10.3 实现报告同步
- [~] 实现 Markdown → JSON 同步 *（v2.91 取消：双产物已由 Runner 自动写出，人工回改 Markdown 反向同步收益低、易与自动流转冲突）*
- [~] 实现同步确认机制 *（随上项取消）*

**验收标准**：
- ✅ 报告正确存储在独立目录
- ✅ 可以在报告中心查看历史产物和运行指标
- ✅ 可以系统打开/定位/删除报告；内置预览与下载已按 v2.91 决策取消（改调系统默认程序）
- ⛔ Markdown → JSON 同步已取消（v2.91）

---

### 阶段 11：应用生命周期（Day 26-27）

#### 11.1 实现首次启动引导
- [ ] 检查 Python 环境
- [ ] 创建 virtualenv 并安装依赖
- [ ] 引导用户配置 LLM
- [ ] 创建示例画布和 Agent
- [ ] 显示新手引导（3 步教程）

#### 11.2 实现关闭确认
- [x] 检测是否有任务正在运行（Runner 向 Tauri 同步任务运行态）
- [x] 任务运行中拦截关闭并提示用户先中止或等待完成
- [x] 清理资源（窗口销毁时停止托管 Python 子进程）

#### 11.3 实现日志系统
- [ ] 实现详细日志记录（输入、输出、错误、时间戳）
- [ ] 日志按日期分文件（`logs/{date}.log`）
- [ ] 日志格式规范

#### 11.4 实现数据版本管理
- [ ] 所有 JSON 配置添加 version 字段
- [ ] 实现版本检查逻辑
- [ ] 实现迁移脚本框架
- [ ] 实现自动迁移（应用启动时）

#### 11.5 实现开发启动稳定性
- [x] 修复 Tauri 开发模式白屏（新增 `devCsp`，放行 Vite 内联脚本/热更新 WebSocket，仅影响开发模式）
- [x] 修复开发服务 React Refresh 重复注入（`scripts/dev-server.mjs` 不再重复加载 React 插件）
- [x] 开发地址统一为 `http://127.0.0.1:5173`，避免 `localhost` 解析差异

**验收标准**：
- ✅ 首次启动引导流程完整
- ✅ 运行中关闭保护正常
- 🔶 生命周期核心保护已落地；首次启动引导、完整日志系统、完整数据版本管理待续

---

### 阶段 12：测试与部署（Day 28-30）

#### 12.1 功能测试
- [x] 前端静态检查（`npm.cmd run lint` 已通过）
- [x] 前端生产构建测试（`npm.cmd run build` 已通过；仅有 bundle 体积提示）
- [x] Python 服务冒烟测试（依赖导入、`/health`、`GET /tools`、`file-read` 最小执行调用均通过）
- [x] 技术评审快捷修复验证（Python `py_compile`、前端 `npm.cmd run lint/build` 均通过；本轮覆盖 CORS、拖拽解析、搜索链接过滤、生产调试入口隐藏、请求/文件处理上限、弹窗卸载守卫、迁移容错与滚动体验）
- [x] 总 Agent 上下文压缩 MVP 验证（不引入新依赖；长对话旧消息后台压缩为会话摘要，后续只携带摘要 + 近期原文历史；摘要成功后会用轻量提示告知用户；`npm.cmd run lint/build` 通过）
- [x] 节点产物摘要 MVP 验证（Runner 本地生成产物摘要并保存到 `lastOutput.items.summary`；右侧属性面板展示摘要；下游节点接收摘要提示但仍保留完整上游内容；`npm.cmd run lint/build` 通过）
- [x] Tauri 开发白屏修复验证（确认开发 HTML React Refresh 从重复注入恢复为单份；`tauri.conf.json` 新增开发专用 `devCsp`，生产 CSP 不放宽；`npm.cmd run build` 与 `cargo check` 通过）
- [ ] 测试画布操作（创建、保存、加载、撤销/重做）
- [ ] 测试 Agent 系统（创建、编辑、删除、克隆、导出/导入）
- [x] 测试工具系统（5 个用户工具 + 1 个内部删除工具；`GET /tools`、`docx-read`、`llm-calling`、`file-write` 项目内 `outputs/` 固定落盘已验证；历史外部路径写入能力保留在工具层，但 Runner 不再使用节点自选目录，避免外部目录权限不稳定）
- [x] 测试 Agent Runner（单 Agent 执行；`test` 画布已真实运行成功）
- [x] 测试 Workflow Engine（单画布顺序/依赖并行/失败隔离；多画布并行待测）
- [ ] 测试总 Agent（自然语言控制）
- [x] 测试报告管理（查看历史列表、打开/定位/删除命令链路；内置预览/下载待测）

#### 12.2 边界测试
- [ ] 测试并发限制（6 个画布同时运行）
- [ ] 测试文件大小限制（10MB 输出、2MB 上传）
- [ ] 测试记忆库容量（10,000 条）
- [ ] 测试 LLM 会话隔离
- [ ] 测试 Python 服务崩溃恢复

#### 12.3 打包部署
- [ ] 配置 Tauri 打包
- [ ] 生成 ZIP 压缩包
- [ ] 编写安装指南（`docs/setup.md`）
- [ ] 编写使用说明（`docs/usage.md`）

#### 12.4 最终验收
- [ ] 在干净环境测试安装
- [ ] 测试所有核心功能
- [ ] 修复发现的问题
- [ ] 发布 v1.0.0

**验收标准**：
- ✅ 所有功能测试通过
- ✅ 边界测试通过
- ✅ 可以在干净环境安装运行
- ✅ 文档完整

---

## 里程碑

> **进度快照（2026-07-05）**：M1、M2、M3 已完成；M4 Agent 系统核心完成（Agent 导出/导入 v2.78/v2.79、画布级导出/导入 v2.92 均已落地；Agent 自定义 schema 编辑 UI 延后、代码接口保留）；M5 Python 服务核心已落地并完成冒烟验收（FastAPI、/health、Tauri 启动/停止、服务状态检测、venv 修复与依赖导入、崩溃后最多 3 次自动重启、标题栏手动重启后台；工具服务端口已从旧 8080 切到 18081，避免无法接管的旧后台继续拦截请求；端口已有可用后台时 Rust 会复用该服务而不是误判失败；Tauri HTTP 权限与 CSP 已同步放行 18081；`tauri:dev` 启动链路已修复，绕开 npm.ps1 执行策略、5173 残留端口、HTTP cookies 缓存写入与 log 文件占用问题）；M6 工具系统核心已落地并完成调用验收（静态注册表、GET /tools、POST /tools/{tool_name}/execute、file-read/file-write/docx-read/docx-write/excel/pdf-read/llm-calling 七个用户工具 + 内部 file-delete、路径沙箱与错误分类、项目内 `outputs/` 固定落盘已验证；外部输出目录权限修复保留在工具层但 Runner 不再依赖），工具库已同步展示为「已落地」并改为后台轮询服务状态，但工具执行日志未完成；M7 Agent Runner MVP 已接入并真实跑通 `test` 画布（依赖图执行、前序/文件输入、运行前检查服务端所需工具是否已注册、按文件类型读取 `.docx/.pdf/.xlsx/文本`、LLM 调用瞬断/429/5xx 重试和节点级临时失败补跑、节点属性支持选择 Markdown/Word/Excel/思维导图输出格式并分别生成 `.md/.docx/.xlsx/.html` 到分级 `outputs/YYYY-MM/YYYY-MM-DD/画布运行名/节点产物文件夹/`、同目录 `data.json` 结构化信封、内置输出 schema 校验与一次自动修复、属性面板通过后端 `open_output_dir` 命令一键打开输出目录且按钮已居中、运行副本只读执行、运行副本 `lastOutput` 回填、节点/画布运行状态可视化、运行中/成功/失败节点耗时样式、运行中历史画布自动打开与实时同步、运行页签状态展示和关闭/删除保护、中止任务与产物保留/移除；软件移除或用户手动删除文件后，历史产物名称均会保留为灰色删除线且不可打开；运行历史列表已改为左侧 2px 莫兰迪色竖线展示整张画布状态，整行保持白底；原画布不再被运行状态或产物污染；历史产物打开已补系统打开能力），并新增隔离的标题栏临时状态调试入口；M8 Workflow Engine 已部分完成（单画布顺序/依赖并行/上游 JSON 优先流转/失败隔离）；M10 报告管理 MVP 已完成（按节点查看历史报告、刷新、打开、定位、删除），M12 打包部署仍未开始；M9 总 Agent 已完成聊天闭环、模型配置、联网搜索、记忆、多会话和系统提示词导入，尚未实现自然语言操作解析/确认/工具管理。当前前端 lint/build、Rust `cargo check`、Python `py_compile/unittest`、`tauri:dev` 启动验证和 `test` 真实运行均已通过；格式产物已在 `outputs/_format_smoke/` 冒烟生成 docx/xlsx/html。

> **v2.63 进度同步（2026-07-05）**：M8 Workflow Engine MVP 已补齐多画布批量并行、单画布 4 节点并发、80ms 启动错峰、节点级临时失败 2 次重试、Agent/节点输入输出 JSON Schema 文本配置、跨节点 schema 匹配校验与 LLM fallback 转换；M9 总 Agent 已补自然语言操作解析与确认式执行，可创建画布、重命名当前画布、创建 Agent、运行当前画布、并行运行打开画布。已通过 `npm.cmd run build` 与 `cargo check`。

> **v2.64 审计快修同步（2026-07-05）**：按“高危优先、低成本顺手修、低危高成本暂缓”处理 v4 技术评审报告。已修复 SSRF 非白名单公网放行，LLM 代理只允许可信域名；已移除文件写入工具的 UAC 提权修复路径，避免临时脚本以管理员权限修改 ACL；已用 `asyncio.to_thread` 包装 LLM 同步 HTTP 调用，缓解 FastAPI 事件循环阻塞。同步更新 Python 服务版本握手与回归测试。已通过 Python `py_compile/unittest`、`npm.cmd run lint/build`、`cargo check`。暂缓项：canvasStore 拆分、dirty hash、recomputeDerived 增量化、组件拆分、搜索状态持久化等低危高成本优化。

> **v2.65 自然语言规划同步（2026-07-05）**：M9 从规则解析升级为 LLM 结构化规划。总 Agent 会在操作类话术中调用选定模型输出受控 JSON 计划，确认卡片逐条展示计划步骤，确认后由本地白名单执行器落地；模型不直接改状态。当前支持创建画布、重命名、创建 Agent、添加节点、连接节点、删除节点、设置节点输出格式、运行当前/全部画布；规划失败时保留原规则兜底。已通过 `npm.cmd run lint/build` 与 `cargo check`。

> **技术评审快修同步（2026-07-04）**：已完成一批不改架构的护栏修复，包括 CORS 收紧、拖拽异常容错、搜索链接协议过滤、生产环境隐藏临时调试入口、请求体/批量删除/Excel 读取/PDF 图片提取上限、工具配置弹窗卸载守卫、总 Agent 迁移容错、聊天自动滚动体验与状态栏脏检查优化；当时暂缓的 SSRF 严格白名单、UAC 提权路径、异步 HTTP 包装已在 v2.64 完成，三处厂商配置合并、组件/Store 大拆分仍按低危高成本项暂缓。

> **上下文压缩同步（2026-07-04）**：总 Agent 已落地最小版长对话压缩机制。每条会话保存一份旧上下文摘要和已摘要消息计数；当消息数量与文本长度超过阈值时，使用当前对话模型在后台生成摘要，失败不影响正常聊天；下一轮对话会自动带上“较早对话摘要”，近期消息仍保留原文。该版本先服务总 Agent，对 Workflow Engine 与节点产物摘要暂不展开。

> **上下文压缩提示同步（2026-07-04）**：每次旧对话成功压缩后，界面会弹出轻量提示告知用户压缩已发生；提示不写入聊天消息，避免影响后续上下文。

> **节点产物摘要同步（2026-07-05）**：Agent Runner 已为每个节点产物生成轻量本地摘要并随 `lastOutput` 写入运行副本/历史快照；右侧属性面板在产物名称下展示摘要；下游节点会收到上游摘要提示，同时继续携带完整上游内容，先提升可读性与后续编排基础，不改变现有产物文件和打开逻辑。

> **连续工作流 LLM 调用修复（2026-07-05）**：针对第二个节点连续调用模型时只显示“工具执行失败”的问题，`llm-calling` 已补 429/5xx 自动重试、`retry-after` 等待和未知异常可读化；Runner 在连续节点调用前加入短暂间隔，避免请求过密。已通过 Python `py_compile`、`npm.cmd run lint/build`。

> **test 画布四格式真实运行同步（2026-07-05）**：已按 Markdown、Word、Excel、思维导图四种格式逐次保存 `test` 画布的「测试用例生成器」节点并运行；四条成功历史分别生成 `.md/.docx/.xlsx/.html` 产物，历史快照均写回产物路径。Excel 已读回表头与样例行，Word 已读回正文样例；思维导图首轮模型空回复后重试成功。

> **运行中闪退/关闭保护同步（2026-07-05）**：补齐应用级运行态保护。Runner 执行期间会向 Tauri 同步任务运行状态；用户关闭主窗口时 Rust 侧会阻止关闭并让前端弹出提示，避免运行中无提示退出；若 Tauri 主进程发生 panic，会写入 `data/crash.log` 供后续排查。已通过 `npm.cmd run lint/build` 与 `cargo check --offline`。

> **异常运行态恢复同步（2026-07-05）**：针对重启后仍显示“运行中”且无法中止的历史副本，新增启动恢复机制。应用加载数据后会自动识别遗留 `running/lockClose` 状态，把运行副本和运行历史改为“应用已重启，任务已中断”，运行中节点标记失败、等待节点标记跳过，并解除页签锁定。已通过 `npm.cmd run lint/build` 与 `cargo check --offline`。

> **异常运行态防复发同步（2026-07-05）**：进一步加固恢复流程。主界面会等“持久化数据加载 + 遗留运行态清理”完成后才渲染，避免短暂显示僵尸运行态；恢复逻辑已抽到数据层通用函数，打开旧历史记录时若仍含 `running` 状态，也会兜底转为“应用已重启，任务已中断”而不是重新锁住页签。已通过 `npm.cmd run lint/build` 与 `cargo check --offline`。

> **LLM 瞬断重试分支修复同步（2026-07-05）**：修复 `requests.ChunkedEncodingError` 异常类路径错误。当前 requests 版本中该异常位于 `requests.exceptions.ChunkedEncodingError`，此前只有在模型接口分块响应中断时才会触发，所以四格式成功运行未覆盖到该隐藏分支；现已补回归检查，瞬断会进入重试/中文提示，不再报 `module 'requests' has no attribute 'ChunkedEncodingError'`。已通过 Python `py_compile`、分支回归检查、`npm.cmd run lint/build` 与 `cargo check --offline`。

> **LLM 异常分支测试护栏同步（2026-07-05）**：新增 `python/tests/test_llm_calling.py`，用标准库 `unittest` 覆盖 `llm-calling` 的分块响应中断、超时、连接重置、HTTP 429 重试、非 JSON 响应错误提示。以后改 LLM 调用/重试逻辑，不能只跑 `py_compile`，必须同时运行 `python/venv/Scripts/python.exe -m unittest discover -s python/tests -p "test_*.py"`。本轮已通过 4 个 unittest、Python `py_compile`、`npm.cmd run lint/build` 与 `cargo check --offline`。

> **画布页签选中态优化同步（2026-07-05）**：增强画布页签当前选中状态，增加蓝色顶边、内阴影、浅底色、文字加粗和关闭按钮对比；运行历史的红/绿/黄状态色保留为状态提示，但选中态由统一蓝色主视觉承担，避免普通画布与运行历史页签肉眼难以区分。已通过 `npm.cmd run lint/build`。

> **画布页签视觉返工同步（2026-07-05）**：根据示意图将选中页签改为浮起白色标签样式，顶部使用低饱和莫兰迪蓝弧线，配合浅边框、柔和阴影和深色文字，降低高饱和蓝色带来的突兀感。已通过 `npm.cmd run lint/build`。

> **画布页签即时切换与方案预览同步（2026-07-05）**：针对页签切换后选中态颜色约半秒才渲染的问题，已禁用 Ant Tabs 页签动画并移除画布页签选中态相关过渡动画，让点击后的选中反馈即时出现；同时新增 `docs/tab-style-options.svg`，提供四套莫兰迪蓝选中态候选方案，待用户选择后再替换正式视觉样式。已通过 `npm.cmd run lint/build`。

> **画布页签 B 方案落地同步（2026-07-05）**：已按用户选择落地 B 方案。当前选中页签采用白底、轻边框、柔和阴影和左侧 3px 莫兰迪蓝书签线；运行历史页签的成功/失败/运行中/中止状态只保留为低饱和图标色，文字保持克制，避免状态色和选中态互相抢视觉。已通过 `npm.cmd run lint`，`npm.cmd run build` 首次遇到 Vite/Rolldown 偶发 HTML 输出路径错误，复跑通过。

> **画布页签宽度修正同步（2026-07-05）**：修正短标题页签被固定最小宽度撑开的问题。当前页签按内容自适应宽度，短标题不再留下大片空白；长标题会在最大宽度内省略，避免历史副本名过长挤占页签栏。已通过 `npm.cmd run lint/build`。

> **Python 后台版本握手同步（2026-07-05）**：针对前台复用旧 Python 后台导致 `llm-calling` 已修复问题再次出现的情况，`/health` 已新增 `serviceVersion`；Runner 运行前会校验后台工具版本，发现旧后台或后台不可达时自动触发重启/拉起，并在版本确认正确后再执行。已清理占用 18081 的旧监听进程，当前健康检查返回新版版本号。已通过 Python `py_compile`、`unittest`、`npm.cmd run lint/build`。

> **模型通信测试语义修正同步（2026-07-05）**：模型配置中的“通信测试”不再只以拉取模型列表作为通过标准；当配置下已有模型时，会通过 Python `llm-calling` 发起一次真实短对话，和画布运行使用同一后端通道。LLM 断流错误也会提示短测试与长任务的区别，并附带请求体大小，方便判断是否是长输入/长输出导致供应商断流。后台版本号已抬升，运行前版本握手会刷新到新代码。已通过 Python `py_compile`、`unittest`、`npm.cmd run lint/build`。

> **输出分级与并行调度同步（2026-07-05）**：Runner 产物已从平铺 `画布_节点_时间戳` 改为 `outputs/YYYY-MM/YYYY-MM-DD/画布运行名/节点产物文件夹/文件`，同名节点会追加输出格式或序号区分；属性面板输出目录预览已同步。单画布 Workflow Engine 已支持依赖图调度：顺序链路按依赖执行，依赖满足的旁支最多 4 个并发运行，启动错峰 80ms；断流、空回复、超时、429/5xx 等临时 LLM 错误会补跑失败节点 2 次，失败节点只跳过自己的后续依赖，不影响无依赖旁支。多画布批量运行已接入，最多 6 个打开画布纳入批次、当前 3 个画布并发。已通过 `npm.cmd run build` 与 `cargo check`。

> **结构化数据流转同步（2026-07-05）**：Runner 已为每个节点同步写出同目录 `data.json`，作为机器可读结构化信封，包含画布/节点信息、输出格式、摘要、主产物路径、结构化数据与原始模型回复；Markdown 输出会生成段落结构，Word/Excel/思维导图沿用已解析 JSON。右侧产物列表可查看并双击打开 `data.json`；下游节点输入优先包含上游机器可读 JSON，再附摘要和文本兜底。Agent/节点输入 schema 已接入，当前支持基础 JSON Schema 校验；不匹配时由下游节点模型转换为目标 JSON。已通过 `npm.cmd run build` 与 `cargo check`。

> **内置与自定义 schema 校验同步（2026-07-05）**：Runner 已在写文件前对 Word/Excel/思维导图/Markdown 的结构化数据执行内置 schema 校验；JSON 不合格时会用当前节点模型自动修复一次，只接受修复后的纯 JSON。Excel 校验 `headers` 与二维 `rows`，Word 校验 `title/sections`，思维导图递归校验 `title/children`，Markdown 校验 `title/text/paragraphs`；Word 修复失败仍保留文本转 Word 的兜底结构。Agent/节点自定义输出 schema 已接入提示词与落盘前校验。已通过 `npm.cmd run build` 与 `cargo check`。

> **报告管理 MVP 同步（2026-07-05）**：新增 Tauri 报告扫描命令，只读取项目 `outputs/` 下的 `data.json` 信封并可按节点名过滤；新增受限删除命令，只允许删除 `outputs` 内报告文件并清理空文件夹。右侧属性面板新增“历史报告”区，点击节点后按时间倒序展示历史产物，支持刷新、打开主产物、打开 `data.json`、定位文件夹和二次确认删除。内置 Markdown/HTML 预览和下载按钮仍待续。已通过 `npm.cmd run build`、`cargo check` 与 Python `unittest`。
> **开发模式白屏修复同步（2026-07-05）**：确认白屏根因是 Tauri 开发模式套用生产 CSP，拦截 Vite React Refresh 内联脚本/热更新连接，同时自定义 `dev-server.mjs` 与 `vite.config.ts` 重复加载 React 插件导致 React Refresh 注入两次。已改为开发窗口使用 `http://127.0.0.1:5173`，新增仅开发模式生效的 `devCsp` 放行 Vite 必需脚本与 WebSocket，生产 CSP 保持严格；`dev-server.mjs` 去掉重复 React 插件。已验证开发 HTML React Refresh 恢复单份注入，`npm.cmd run build` 与 `cargo check` 通过。

| 里程碑 | 完成日期 | 交付物 | 状态 |
|--------|----------|--------|------|
| M1：项目基础搭建 | Day 2 | 可运行的项目框架 | ✅ 完成（Tauri 已接入起窗口；数据持久化改为项目内明文 JSON，可随项目打包迁移；主目录环境配置器框架就位） |
| M2：UI 框架搭建 | Day 4 | 主界面布局 | ✅ 完成 |
| M3：画布系统 | Day 7 | 可操作的画布 | ✅ 完成（撤销/框选/复制粘贴/子节点折叠/连线选中态颜色区分/Ctrl+F 节点搜索已补齐；重做按需求暂不做，节点运行态待执行引擎） |
| M4：Agent 系统 | Day 10 | Agent 管理功能 | 🔶 核心完成（Agent 库定义 CRUD/克隆/删除引用检查、3 预置模板、配置弹窗；两层属性模型：拖入画布快照复制为独立实例、右侧「属性」面板编辑实例、双击节点不做任何事；节点实例「数据来源/输出目录/本次输出」三块 UI+数据层[前序优先自动清除、文件名/URL 手动来源、输出目录预览为 `YYYY-MM/YYYY-MM-DD/画布运行名/节点产物文件夹`]；输入/输出 schema 编辑 UI 延后（代码接口保留，v2.91 决策）；**Agent 导出/导入已在 v2.78/v2.79 落地、画布级导出/导入已在 v2.92 落地**（`canvasTransfer.ts` 信封连带 Agent 快照，导入仅落节点实例不入库）；选文件[存真实路径]已用 `plugin-dialog` 实现，双击打开产出已用系统打开能力实现；网页抓取仍待后续） |
| M5：Python 服务 | Day 12 | 工具执行环境 | ✅ 完成（FastAPI 主应用、CORS、`/health`、Tauri 启动/停止 Python 子进程、服务状态检测、venv 冒烟验收、崩溃后最多 3 次自动重启已实现；新增标题栏「重启后台」按钮；工具服务端口已改为 18081，绕开无法接管的旧 8080 后台；端口已有可用后台时会复用该服务；`tauri:dev` 启动链路已修复；**v2.88 stdout/stderr 已落盘 `logs/python.log`[5MB 滚动 + PYTHONUNBUFFERED]**；v2.89 冷启动清端口孤儿 + 单实例锁 + 退出守卫杀子进程） |
| M6：工具系统 | Day 15 | 6 个内置工具 | ✅ 完成（`router.py` 静态注册表、`GET /tools`、`POST /tools/{tool_name}/execute`、路径沙箱、错误分类已实现；**v2.81 工具已合并为 `file`[文件读写删]/`docx`[Word]/`excel`/`pdf-read`/`llm-calling`，旧名保留只读别名兜底**；Runner 固定写入项目 `outputs/`，外部输出路径能力保留在工具层；**v2.88 工具执行日志已落地**[`router.py` 每次执行记 INFO：工具名+耗时 ms+成功/失败]；**v2.73 起支持姬子动态生成/安装自定义工具**[人工审阅确认代码后落盘装依赖注册]） |
| M7：Agent Runner | Day 18 | 单 Agent 执行 | 🔶 MVP 完成（新增 `agentRunner.ts`，标题栏运行按钮已真实执行节点：点击运行先检查 Python 服务端所需工具，再生成只读运行副本；拓扑排序、前序/文件输入、按扩展名读取 Word/PDF/Excel/文本、LLM 调用带瞬断/429/5xx 重试和节点级临时失败补跑；节点可选择 Markdown/Word/Excel/思维导图输出格式，分别写出 `.md/.docx/.xlsx/.html` 到分级产物目录，同时写出机器可读 `data.json` 并回填 `lastOutput`；写文件前执行内置输出 schema 校验，不合格时自动修复一次；已支持 Agent/节点自定义输入输出 schema 文本、输出校验与输入 schema 转换；运行状态和产物均落在副本/历史记录上，原画布不被污染；运行中/成功/失败节点耗时样式、运行页签状态展示和关闭/删除保护、中止任务与产物保留/移除） |
| M8：Workflow Engine | Day 21 | 多 Agent 编排 | ✅ 完成 MVP（单画布依赖图调度已落地：顺序执行、依赖满足后的并行执行、上游机器可读 JSON 优先流转、摘要和文本兜底、失败节点只跳过自身后续依赖；当前最多 4 个节点并发、启动错峰 80ms；多画布批量运行已接入，最多 6 个打开画布纳入批次、当前 6 个画布并发；schema 匹配校验与 LLM fallback 转换已接入；错误日志仍待续） |
| M9：总 Agent | Day 24 | 自然语言控制 | 🔶 MVP 完成（模型配置中心 + 聊天对话闭环[发送/消息列表/附件/联网搜索+测试按钮/记忆检索/**多会话管理**/显示名改「姬子」+可配置系统提示词弹窗]已完成；自然语言操作解析/确认式执行已接入[双路径正则+LLM 规划器，8 类动作：创建画布、一键建工作流画布、重命名当前画布、创建 Agent、加节点、连线、删节点、改输出格式、运行当前画布]；批量运行打开画布入口已按当前策略移除；v2.73 工具管理已落地：姬子自然语言生成工具→完整代码人工审阅确认→动态注册落盘装依赖，工具库自定义分组+试运行、内置/自定义统一元数据与标签；仅剩删除画布等未建模动作待续） |
| M10：报告管理 | Day 25 | 报告查看功能 | ✅ 完成（报告中心可查看历史报告列表，支持刷新、打开主产物/`data.json`、定位文件夹、二次确认删除；**v2.91 决策取消**内置 Markdown/HTML 预览与下载按钮——改调系统默认程序打开，Markdown→JSON 反向同步同步取消） |
| M11：应用生命周期 | Day 27 | 完整应用 | ✅ 基本完成（项目内 JSON 持久化、Tauri Python 子进程启动/停止、环境配置器脚本已实现；任务运行中关闭主窗口会被拦截并提示，Tauri 主进程 panic 会写入 `data/crash.log`；异常退出/重启后的遗留运行中历史会在主界面渲染前自动标记为中断并解除锁定，打开旧历史也有兜底恢复；Tauri 开发模式白屏已通过 `devCsp` 和开发服务去重修复；**v2.88/v2.89 完整日志系统已落地**[Python `logs/python.log` + Rust `logs/tauri.log` 双侧 5MB 滚动]；**v2.89 稳健三件套**[退出守卫 `RunEvent::ExitRequested` + 冷启动清端口孤儿 + 日志落盘] + **单实例锁**[防双开抢写] + **窗口尺寸/位置/最大化持久化** + **标题栏常驻服务状态指示灯**[绿/黄/红三态]；仅首次启动引导未做[可选]） |
| M12：测试与部署 | Day 30 | v1.0.0 发布 | 🔶 打包链路就绪（**v2.90 Windows NSIS 安装包方案已落地**：`python/build-dist.mjs` 组装可重定位独立 CPython 3.12[已实测生成 python-dist 261MB] + `tauri.conf.json` 配 `bundle.resources`/`nsis.installMode:"currentUser"`/`targets:["nsis"]` + `python_manager.rs` release 解释器子路径改 `runtime/python.exe`；`cargo check --offline` 已过。**待用户手动执行**：`node python/build-dist.mjs` → `npm run tauri:build` 出 `.exe` 安装包 + 干净机器端到端安装测试；未签名首次运行可能触发 SmartScreen。前端自检 `npx tsc -b`/`npx oxlint` 持续通过） |

---

## 风险与应对

### 风险 1：Python 环境配置复杂
- **影响**：用户无法使用工具
- **应对**：提供详细的安装指南，自动检测和配置

### 风险 2：LLM API 调用不稳定
- **影响**：Agent 执行失败
- **应对**：实现重试机制，提供错误提示

### 风险 3：画布性能问题
- **影响**：节点过多时卡顿
- **应对**：限制节点数量，优化渲染

### 风险 4：数据格式变更
- **影响**：旧数据无法使用
- **应对**：实现数据版本管理和迁移机制

---

## 备注

- 每个阶段完成后进行一次代码审查
- 每完成一个里程碑进行一次功能演示
- 遇到问题及时调整计划
- 保持与开发清单的一致性

---

**施工清单创建完成，准备开始施工！**
