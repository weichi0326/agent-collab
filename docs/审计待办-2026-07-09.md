# 审计待办 - 2026-07-09

本文件只记录本轮暂不修的工程待办，不是用户自述文件。

## 本轮已同步（v3.02）

- 已把姬子近期能力/UI 改动同步到开发清单、施工清单、交接注意事项、技术评审报告、全期统计和开发全记录。
- 本轮同步的代码事实：已有模型配置会重新识别能力；MiMo v2.5 系列能力规则补齐；姬子外部状态栏变轻；任务计划和一键体检放进姬子抽屉；Skill 商店取消“自建”页签，改按 Skill 内容自动分组。
- 已记录验证口径：`npm.cmd run lint`、`npm.cmd run test -- --run`（28 passed）、`npm.cmd run build` 通过；浏览器插件验证连续超时，不能写成已通过。

## 仍需注意

- 模型能力识别仍是“规则 + 模型名线索”的自动推断，不等于厂商官方保证；用户仍应能手动修正能力标签。
- Skill 自动分类只是管理界面分组，不改变模型真正读取 Skill 的内容。

## 待办

1. GitHub CI
   - 现状：仓库还没有 `.github/workflows`。
   - 建议：上 GitHub 前补 CI，自动跑前端 lint/test/build、Rust `cargo check`、Python unittest。

2. 发布前同步 `python-dist`
   - 现状：`app/src-tauri/python-dist/` 是构建产物，不入库；源码 Python 改动后，发版前必须重新生成或 sync-only 同步。
   - 建议：打安装包前固定执行 `node python/build-dist.mjs --sync-only` 或完整重建。

3. 前端真拆包
   - 现状：构建能通过，但 Vite 提示多个动态导入无效，主包偏大。
   - 建议：功能稳定后，把报告中心、配置弹窗、工具库等改成真正的 `React.lazy` 按需加载。

4. 旧 planner 与 JSON 工具清理
   - 现状：`masterPlanner.ts` 里仍保留旧规划入口，多个文件有类似 `cleanJsonFence` 逻辑。
   - 建议：抽一个公共 JSON 文本解析工具，再确认旧 planner 是否可删除，避免以后改错入口。